/target
/target-*/
/docs/book/book
/docs/book/src/playground
/crates/zed-brink/target
CLAUDE.md.bak
.DS_Store
crates/brink-web/www/pkg/
node_modules/
packages/brink-studio/dist/
packages/brink-studio/dist-embed/
test-results/

# Catch build artifacts under any excluded sub-crate (e.g. zed-brink, benchmark drivers)
**/target/

# Per-machine Claude session tooling (worktrees, preview launch configs)
.claude/*
!.claude/skills/
!.claude/skills/**
# Versioned so cloud/approval-gated sessions carry a permission allowlist and
# pump agents don't park on the classifier. Consistent with the existing
# deny-by-default posture (one more explicit carve-out); does NOT settle #1803.
!.claude/settings.json
packages/wasm/dist/
packages/ink-editor/dist/

# npm lockfile — this is a pnpm workspace (pnpm-lock.yaml); an npm
# install at the root creates a stray package-lock.json.
/package-lock.json

# Design-canvas scratch (seeded payloads; durable sources live in docs/design/)
.design/

# Recorded performance runs (probe JSON + CDP traces; measure-first ruling
# 2026-08-24). Artifacts are per-machine measurements — the durable summary
# lives in docs/desktop-perf-baseline.md.
perf-runs/

# brink-prose's wasm-pack output, same as brink-web's (built, never committed).
crates/brink-prose/www/pkg/
