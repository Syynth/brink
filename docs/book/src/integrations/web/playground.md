# Playground

Try brink in your browser — this is the [Web & WASM](./index.md) build running
live: write ink on the left, play it on the right. It's the same `EditorSession`
+ `StoryRunner` surface a full client uses, with no install.

<iframe src="../../playground/index.html" style="width:100%;height:80vh;border:none;"></iframe>
