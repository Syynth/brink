# Ruling ledger

**What this is.** A derived view over `docs/decision-log.md`: for each ruling that
was traced, what actually *happened* to it in the repo. The decision log records
what we decided and when, and stays authoritative for content — it is immutable
and is not edited by this workflow. This file records the fate of those decisions
against the tree, and is regenerated by the ruling-ledger-audit workflow.

**This run:** 2026-07-27, against `origin/main` @ `ce52c272300473cf0b85efca7b66233a5f9df8ca`.

**Verdicts.**

| Verdict | Meaning |
|---|---|
| **ORPHANED** | The commitment is not built and no open issue owns it. Actionable. |
| **CONTRADICTED** | Main disagrees with the ruling as written. Actionable. |
| **TRACKED** | Not built, but an open issue owns it. Correctly parked. |
| **DELIVERED** | The commitment is in the tree (or, for process rulings, in the binding instruction surface). |
| **SUPERSEDED** | A later ruling overrode it; the code follows the later ruling. |
| **UNKNOWN** | Status could not be determined. A first-class verdict — see the note. |

## Counts

| Verdict | Rows |
|---|---:|
| ORPHANED | 29 |
| CONTRADICTED | 13 |
| TRACKED | 22 |
| DELIVERED | 203 |
| SUPERSEDED | 16 |
| UNKNOWN | 0 |
| **Total rows** | **283** |

`docs/decision-log.md` at this ref contains **292** entries (`grep -c '^## '`).
Rows are not 1:1 with entries: the extraction pass filtered out entries that were
pure records of changes already made, and conversely split several entries into
per-sub-commitment rows (the R1 entry became 6 rows; the T1c draft-round entry
became 2; eight titles appear twice where two readings were traced separately).
Netting the splits, the 283 rows cover roughly 270 distinct entries.

## What this run verified

- **Every ORPHANED and CONTRADICTED citation below was re-opened at
  `ce52c272` by this pass** — 42 primary `file:line` citations plus the
  absence-greps and issue states they rest on.
- **~85 further `file:line` citations** across DELIVERED / TRACKED / SUPERSEDED
  rows were re-opened in bulk, and **~170 cited PR numbers** were checked for
  merge state.
- Remaining table evidence is transcribed from the tracing pass. Where a row's
  evidence has not been re-read by this pass, treat it as tracing-pass testimony,
  not as a citation this run stands behind.

Two corrections this pass found in the traced evidence:

- `crates/brink-lsp/src/backend.rs`: `struct ConfigOverrides` is at **:911**, not
  :916. The field set is as reported (`dialect`, `types`, `lints`, `deny_warnings`).
- **PR #1659 and PR #1666 are OPEN, not merged.** Both were cited in tracing notes
  in a way that reads as landed. The "angle brackets over `Option[T]`" decision-log
  entry did land, but via merged **PR #1663** (commit `0d95ec3ea`); #1659 is a
  separate still-open ruling PR. #1666 (`#1446` branch-expansion ruling) has not
  landed at all.

Headline numbers re-read at this ref, since a prior audit got them wrong:
`RATCHET_EPISODE_COUNT: usize = 5607`
(`crates/internal/brink-test-harness/tests/oracle_snapshots.rs:54`); 6,639
`*.oracle.json` files under `tests/`; `crates/internal/` contains 18 crates and
**none** of `brink-converter`, `brink-json`, `brink-codegen-json`.

---

# ORPHANED (29)

Not built, and no open issue owns them. Ordered most consequential first.

## 1. R1 sub-commitment (a): fix the #1504 miscompile — qualify anonymous scope paths by owning module

**2026-07-27** · `crates/internal/brink-ir/src/lir/lower/mod.rs:487`

A **live miscompile is in main** and its owning issue is closed as COMPLETED.
Every file's root content lowers with an empty scope path
(`crates/internal/brink-ir/src/lir/lower/mod.rs:487` = `String::new()`, against a
shared `IdAllocator`), so anonymous `c-N`/`g-N` container paths collide across
files. The acceptance test exists but is disabled:
`crates/internal/brink-ir/tests/lir_lowering/root_content_definition_id_soundness.rs:68`
carries `#[ignore = "known bug #1504(a): root-content scope paths are unqualified by …"]`.
The R1 ruling explicitly unbundled this — *"a shipping correctness bug must not
sit behind an identity ruling"* — yet **issue #1504 is CLOSED (COMPLETED)**. Open
issue **#1673** is only a codegen duplicate-id *guard*, not the qualification fix.

## 2. CORRECTION to the R1 entry: intl never consults the alias table

**2026-07-27** · `crates/internal/brink-intl/src/compile.rs:43`

Ruled a standing workflow defect that must be fixed; assumed home **#1442 is
CLOSED (COMPLETED)**, closed by docs-only work (PR #1661, whose own body says *"design
deliverable, not an implementation… No identity, id-minting, or matching code
changes here"*). The maintainer comments rescoping #1442 into *the* intl
alias-awareness issue landed **after** it was already closed. Code is unchanged:
zero alias usage anywhere in `crates/internal/brink-intl/src`, and compile-locale
still matches scope ids by string equality —
`crates/internal/brink-intl/src/compile.rs:43` =
`.ok_or_else(|| IntlError::ScopeNotInBase(scope.id.clone()))?`. Characterization
tests **pin the broken behaviour** (`crates/internal/brink-intl/tests/rename_identity.rs`).
`docs/intl-spec.md:417` still points readers at the closed #1442.

## 3. XLIFF `<unit id>` reverts to DefinitionId-based (#1442)

**2026-07-26** · `crates/internal/brink-intl/src/compile.rs:43`; `docs/decision-log.md:2234`

Split outcome. The mechanical half shipped (merged PR #1594 keys unit ids on
DefinitionId). The half this ruling actually owes — intl alias-awareness so a
rename does not orphan every translation — is the finding above: same closed home,
no successor. Additionally the entry still carries **`STATUS: tentative`** at
`docs/decision-log.md:2234` awaiting maintainer confirmation, which has not been
applied at this ref. The sibling R1 gaps all got fresh open homes (#1671, #1672,
#1673, #1674); intl alias-awareness got none.

## 4. R1 ruled: modules-spec §5 STANDS; anonymous-container state is bounded, reported, opt-in-nameable

**2026-07-27** · `crates/internal/brink-ir/src/lir/lower/mod.rs:487`

Three of four gaps are properly tracked (**#1671**, **#1672**, **#1674** all OPEN).
The one item the ruling explicitly unbundled — the #1504 miscompile fix — has no
owner, because **#1504 is closed as COMPLETED while the miscompile is still in
main**. This row exists to record that the unbundling was ruled and then did not
happen.

## 5. The trio is pure-required; eager/lazy dissolved; the weird thing gets the ugly method

**2026-07-18** · absent from `crates/internal/brink-analyzer/src/resolve.rs` and `crates/internal/brink-analyzer/src/infer/intrinsics.rs`

An entire stdlib feature family with **no delivery vehicle at all**. Verified by
grep at this ref: none of `map`, `filter`, `fold`, `filter_map`, `each`,
`map_each` appears in the prelude name roster or the intrinsic-effects table
(`grep -E '"(map|filter|fold|filter_map|each|map_each)"'` over both files returns
nothing). No LIR lowering arm. No issue owns it across any state. No wave owns it
either — `docs/stdlib-sequencing.md` decomposes Track A into A1–A8 and Track B
into B1–B5 and its §5 hosting map has a row for every other verb family but none
for the trio. Its stated assumed home is worse than empty: the *"#672-B property
laws"* it leans on live in **issue #672, CLOSED 2026-07-14 — four days before this
ruling was made.** What survives is only the rule's shadow:
`crates/internal/brink-analyzer/src/comparator_contract.rs` enforces the trio's
pure·silent bound (E119) on `sort_by`/`sorted_by` comparators — the constraint was
applied to a verb that exists while the verbs it was ruled for were never built.

**Missing:** an issue and a sequencing wave for the pure-required trio +
`filter_map` + `each`/`map_each`, including the ruled rejection error that "names
both exits".

## 6. Effect system for the native surface: one unified row, checking-not-handlers, bounds+row-poly

**2026-07-21** · `crates/internal/brink-analyzer/src/infer/effects.rs:59`; `crates/internal/brink-format/src/inkb/read.rs:864`

The design is delivered as spec text (`docs/effects-spec.md` §14, both
reconciliation flags marked RESOLVED). The build is entirely absent and unowned.
Verified: `pub struct EffectRow` at `infer/effects.rs:59` has fields
`reads, writes, calls, opaque, emits, tags, faults, faults_refined` — **no
`suspend`, no `terminates`**; `grep -rn suspend crates/internal/brink-analyzer/src`
returns nothing. The format graduation never happened —
`crates/internal/brink-format/src/inkb/read.rs:864` still rejects reserved bits
3–7. And **§6.1 shallow row-polymorphism, which this ruling explicitly declared
IN-SCOPE not deferred**, is unbuilt; `effects.rs` still documents every call
through a function value as opaque/pessimal.

Checked all open issues: **#1665** covers only the block-as-expression /
effect-signature checker and never mentions suspend, terminates, or §6.1; **#1211**
is the deeper core-calculus redesign this ruling explicitly distinguishes itself
from; **#826** is an unrelated icebox. This is the clean orphan.

## 7. T2 effects rulings (foundations sitting): rows on types, flow mechanisms, factored encoding, trust tiers

**2026-07-13** · `crates/internal/brink-analyzer/src/infer/ty.rs:58` vs closed issue #860 titled "rows on `Ty::Fn`"

Mostly delivered — per-SCC coalescing, factored wire rows with the narrowability
bit, the load-time capability table, trust tiers 1 and 3 all shipped. But the
**headline clause is unbuilt and every owning issue is closed**:
`crates/internal/brink-analyzer/src/infer/ty.rs:58` is `Fn(Vec<Ty>, Box<Ty>)` —
params and return only, no `{reads, writes, calls}` row, and the unifier joins no
rows. `effects.rs` states outright that rows only *conceptually* ride `Ty::Fn`.
So flow mechanism 1 (row variables / effect polymorphism) and mechanism 3 (heap
fallback via the type-level join) are the pessimal floor, not the ruled join.
**#860 CLOSED (COMPLETED), epic #859 CLOSED, #872 closed having shipped only
write-once-local origin tracing.** Trust tier 2 is also unbuilt but is homed at
#1093/#717.

## 8. The block-effect insight distilled: meaning from analysis, not the parser; S2 dropped, #1213 downgraded

**2026-07-21** · `crates/internal/brink-ir/src/hir/types.rs:365`

Bookkeeping half delivered (S2 recorded dropped, both §14.5 flags RESOLVED, #1213
open and unworked). **S3 and S4 are the orphans.** S3 ("consumers read off
`Block`") is unbuilt: the `tail` field's own doc at
`crates/internal/brink-ir/src/hir/types.rs:365-372` says `stmts` *"remains the sole
source of truth for every consumer (analyzer, HIR→LIR lowering, codegen) — `tail`
is redundant-but-correct data, not yet read by anything."* The only analyzer
reference (`native_choice_dead_end.rs:245`) deliberately does **not** read it and
reimplements its own walk. S4 ("wire the ruled effect row + build §6.1") is
finding 6 above. No BE-S3/BE-S4 issue exists (`git log` shows exactly one BE-S
commit, `e4f2df828` = S1) and no open issue names either slice. The migration
chain stalled after S1 with two of four live slices unowned.

## 9. The spelling cluster ruled: companion-module impls, NonEmptyRange, methods-not-operators

**2026-07-19** · `crates/internal/brink-syntax-native/src/lexer/ident.rs` (no `impl`, no `self`); `crates/internal/brink-analyzer/src/protocols.rs:25`

Split verdict; the architectural core is the orphan. **Delivered:** S2
(`NonEmptyRange` refinement, merged PR #1136). S3 holds by absence (no
`contains`/`intersect` operator bound). **Orphaned:** S1+S4 — the Rust-shaped
`impl display for Npc` / `impl Npc { fn greet(self, …) }` companion-module surface
does not exist. Verified: `grep -E '"(impl|self)"'` over
`crates/internal/brink-syntax-native/src/lexer/ident.rs` returns nothing — neither
is a keyword. Worse, `crates/internal/brink-analyzer/src/protocols.rs:25-27` still
documents the spelling as **undecided** — *"## v1 has no impl \*spelling\* … The
implementation spelling (attribute vs impl-block) is ⏳ for the code-dialect
sitting"* — stale against a ruling made the same day, and protocol impls remain
programmatic-only. **Also orphaned:** the compile-checked snake_case/UpperCamel
casing partition — no casing diagnostic exists anywhere.

This matters downstream: **#1541** (flags verbs into the companion module, OPEN)
explicitly rides machinery that has no owner.

## 10. Prose-dialect sitting 1 — element/markup two-layer model, XML spans in the line table (#1351)

**2026-07-25** · `crates/internal/brink-format/src/line.rs:77`; `crates/brink-runtime/src/story/types.rs:29`; `crates/internal/brink-format/src/inkb/mod.rs:75`

Design half delivered (merged PR #1441, `docs/prose-dialect-spec.md` §4.4 "Wire
representation (RULED)"). Build half has no owner and no code. Verified: `pub enum
LinePart` (`line.rs:77`) is `Literal | Slot | Select` — **no `Span` variant**; `pub
enum Line` (`story/types.rs:29`) is `text: String` + `tags` on every variant, no
span surface; `.inkb` is still `VERSION: u16 = 5` (`inkb/mod.rs:75`). No span
normalization on any `source_hash` site; no `Span` in `brink-intl`.

**Hazard:** sitting 1 named recognizer growth (**#1446**) as the prerequisite to
batch with the v6 bump. #1446 was CLOSED 2026-07-27 by a ruling whose successor
**#1667** is scoped to branch expansion for inline conditionals/sequences — so the
"recognizer must admit spans" clause lost its assumed home when #1446 closed.
(Note also that PR #1666, cited in that closure, is still **OPEN** at this ref.)

## 11. Prose-dialect sitting 2 — break-compat, element roles replace glue ceremony, completions doctrine, measurement tiers (#1351)

**2026-07-25** · `docs/prose-dialect-spec.md:177-205` is the only artifact

Three limbs, one tracked and two orphaned. **Tracked:** measurement — **#362 is
OPEN** and was rescoped for exactly this. **Orphaned limb 1** — element
roles/attachment: no role enum, no attachment pass, no dangling-cue diagnostic;
`grep` for `ElementRole` / `attached_forward` over `crates/` returns zero hits.
**Orphaned limb 2** — completions: the ruled *"new project-db usage index
obligation"* for project-wide cue-name harvest has no symbol in `brink-db` and no
issue (`grep` for `UsageIndex`/`usage_index`: zero).

Also: #362's rescope comment sequences the work behind
`docs/implementation-roadmap.md` — **that file does not exist at this ref**, so the
sequencing pointer dangles. And spec §3.7 still says the block id lives "in element
data", which a later ruling reversed (see finding 21).

## 12. Prose-dialect sitting 3 — product vision: the interactive screenplay (#1351)

**2026-07-25** · `crates/brink-runtime/src/story/types.rs:119`

Only the ruling text shipped (docs-only PR #1452). Verified: `pub struct Choice`
(`story/types.rs:119`) is `text`/`index`/`tags` — **no `element` field**; no `Step`
enum, no `OutputLine`, no Fountain/FDX or VO-sheet exporter anywhere in
`crates/brink-runtime` or `crates/internal/brink-format`. No issue owns
`Choice.element`, per-path export, or the §7 output shape — **#1449** (OPEN) is
only its named prerequisite and its own body says it is blocked on the
Step/OutputLine redesign "being scheduled", i.e. the redesign itself is unowned.

Two sub-parts are fine: the editorial trio is chartered to NS-T (#1131), and the
`[]` anatomy re-ratification agrees with code.

## 13. Prose-dialect sitting 4 — header-scoped scenes, [slug], line-neutral diverts, lowered transitions (#1351)

**2026-07-25** · `docs/native-surface-charter.md:81`; `docs/prose-dialect-spec.md:460-529`

Doc half delivered — charter §4 carries the amendment at
`docs/native-surface-charter.md:81-85`, and §8b records all 13 rulings. Build half
has no owner and no code: a case-insensitive grep for `scene|heading|slug` across
`crates/internal/brink-syntax-native/src` and
`crates/internal/brink-ir/src/hir/lower_native` returns **zero hits** — header-scoped
stitch bodies, trailing `[slug]`, header-line `#tag`s, the
`lower: content|call|nothing` column, `scene_entered(title, slug)` codegen, and the
compact cue `@NAME: text` are all unimplemented. No implementation issue exists.

Secondary: the ruling names **#474** (OPEN) as the iceboxed consumer this unblocks;
#474 has zero comments, so the unblock is recorded nowhere on the issue that was
waiting for it.

## 14. Prose sitting 4 addendum — conventions are authored as a brink module, not a data file (#1351)

**2026-07-25** · `crates/internal/brink-environment/src/lib.rs:169`; `crates/internal/brink-ir/src/dialect.rs`

Spec §3.5 rewritten as ruled; nothing else exists. Verified: `pub struct
Environment` (`brink-environment/src/lib.rs:169`) has no frozen-conventions slot;
repo-wide grep for `std::conventions` / `conventions()` returns zero; the CLI
`Commands` enum has no `conventions export`; there is no `brink.toml` pointer key.
The incumbent JSON artifact (`crates/internal/brink-ir/src/dialect.rs`, *"a
versioned, pure-JSON, authoring-time/tooling schema"*) is the form this ruling
demotes to generated interchange — it is unchanged, and `docs/dialect-spec.md`
still presents it as the authored form **with no supersession note**.

Both prerequisites the ruling leans on do exist and agree (`@[effects(pure)]`
parsed and checked; `begin_function_eval` exists).

## 15. Prose sitting 4, addendum 2 — the @[element] annotation surface (#1351)

**2026-07-25** · `crates/internal/brink-ir/src/hir/diagnostics.rs:1468`

Ruled and spec'd (docs-only PR #1456), then nothing. **Writing the ruled surface
today is a hard compile error**: `diagnostics.rs:1468` is E111's message, whose
closed set is *"the `@[…]` channel recognizes `effects`, plus `was` and `allow` on
the native surface"* — `@[element(...)]` and `@[style]` are not recognized. No
capture→parameter binding, no `content`-param fragment routing. The
no-invisible-expansion metadata channel is absent: the existing per-line channel
`DialectLineInfo` (`crates/internal/brink-ide/src/line_context.rs:124-138`) carries
kind/attrs/hidden_spans/content_span only — no matched-handler identity, no capture
spans, no explain-match query. No issue owns any of it, and #1351's comment thread
stops at the sitting-1 checkpoint, so the board has no record that this surface is
now the first thing to build.

## 16. Prose sitting 4, addendum 3 — sigil + name dispatch for annotation elements; @[style] editor hooks (#1351)

**2026-07-25** · `crates/internal/brink-ir/src/hir/lower_native/annotation.rs:37`

Spec half shipped (merged PR #1457, `docs/prose-dialect-spec.md` §3.5b). Code half
has no owner and no builder — and **native lowering explicitly declines it**:
`annotation.rs:37` reads *"`@[element(pattern)]` / `@[style(…)]` (ruled in principle
by the prose sitting-4 addenda)"* and they are deliberately not implemented, so both
names hit E111. No `!name` dispatch exists (`BANG` is only an operator token), no
element/style annotation consumer, no semantic-class emission. Issue search across
all states returns no implementation issue.

## 17. Prose sitting 4, addendum 4 — built-in presentation tokens for @[style] (#1351)

**2026-07-25** · `packages/ink-editor/src/theme.ts:42`

Spec-delivered only (§3.5b enumerates the closed vocabulary: left/center/right,
bold/italic/dim/mono, uppercase, conceal, plus `brink-*` hooks). Nothing implements
it, and **the hardcoded screenplay CSS the ruling says the self-describing preset
replaces is still hardcoded** — `packages/ink-editor/src/theme.ts:42` =
`textAlign: "right"`.

**Assumed-home check failed:** prose-spec §9.6 assigns the token vocabulary as
incoming scope for NS-T (#1131) / NS-T.1 (#1350). Both are OPEN, but **neither body
mentions `@[style]`, presentation tokens, or the vocabulary** — #1350's scope is
`.brink` registration + CST semantic tokens + diagnostics only. The spec points at
issues that do not carry the commitment.

## 18. Prose sitting 5 — cue-only choice typing, tags-as-extensions, Step, open-map element data (#1351)

**2026-07-25** · `crates/brink-runtime/src/story/types.rs:29`; `crates/internal/brink-format/src/line.rs:77`; `crates/internal/brink-syntax-native/src/parser/tests/content.rs:765`

Spec-delivered (§8d, merged PR #1459), code-delivered on none of the eight items.
Runtime output is still `Line` with fused text-bearing terminals — no `Step`, no
`OutputLine`, no block id, no element data, no markup span. The escape set is
unbuilt: `content.rs:765` is a live test asserting *"no escape mechanism yet: `\#`
still opens a TAG"*. Nearest open issues **#1449** and **#1520** are
prerequisites/adjacent; neither owns the redesign.

**Doc drift:** §7 still tags naming as ⏳ although §8d.7 declares it closed, and
§3.7 + §7 still place the block id "in element data", which the later same-day
ruling reverses (finding 21).

## 19. Compile-baked attachment ratified (prose #1351)

**2026-07-25** · `docs/prose-dialect-spec.md:190`

Ratified in the decision log **and nowhere else**. PR #1468's file list contains
exactly one path, `docs/decision-log.md` — the normative spec never absorbed the
ratification. `docs/prose-dialect-spec.md:190` still reads *"**Mechanism (leaned):**
attachment resolves at compile time over the source…"*, so the next reader of the
spec will not know it is ruled. No compiler code: `grep -i cue` over
`crates/internal/brink-ir/src/hir/lower_native` returns nothing, and no
speaker/delivery carrier exists in the compiler or runtime — the only speaker
machinery is editor-side ink-dialect chaining
(`crates/internal/brink-ide/src/line_context.rs:377-394`), which is exactly the
tooling-layer adjacency reconstruction compile-time baking is meant to replace.

**Missing:** spec §3.6 upgraded from "leaned" to RULED, and a build issue for the
attachment pass.

## 20. `!name` dispatch = ordinary module scope + brink.toml implicit usings

**2026-07-25** · `crates/internal/brink-project-config/src/lib.rs:192`; `crates/internal/brink-ir/src/hir/lower_native/annotation.rs:188-215`

Neither half is built and neither half is filed. **(a)** `pub struct ProjectConfig`
(`brink-project-config/src/lib.rs:192`) carries exactly `dialect`, `types`, `lints`,
`deny_warnings`, `unprune_dirs` — **no implicit-usings list**; `grep -rniE "implicit
using|global using" crates/` returns **zero**. **(b)** Native annotation lowering
recognizes only `was`, `effects`, `allow` — no element/style dispatch path. Issue
search for "implicit usings" returns **zero issues**; the stated assumed home is not
an issue at all (charter §13.3 lists "Prelude design" as an unfiled sitting docket
line).

Note the ruling is **already being leaned on elsewhere**: the UFCS ruling
(`docs/decision-log.md:2205`, built as closed #1482) describes resolution scope as
*"file `use` + implicit usings + prelude"* — a scope tier that does not exist in
config or code.

## 21. Block id is a dedicated OutputLine field, not element data

**2026-07-25** · `docs/prose-dialect-spec.md:434`

**The governing spec actively says the opposite.** No code exists yet (the runtime
output type is still `Line`), so this is not contradicted-by-code — but
`docs/prose-dialect-spec.md:434` reads *"`element.data` carries attachment-resolved
payloads (+ block id per §3.7)"* and :201 says *"a baked block id in element data"*.
PR #1468, which recorded the ruling, touched `docs/decision-log.md` **only**. A
builder reading the spec will put the block id in the open map — the exact thing the
ruling forbids. No issue owns the field-vs-map shape: **#1519** names "block id" only
as one of several anticipated v6 sections; **#1449** says the redesign is unscheduled;
**#1351** enumerates it nowhere.

**Missing:** a spec fix at `prose-dialect-spec.md:434` and a build issue.

## 22. Slugify = tooling materializes the slug into the source (prose #1351)

**2026-07-26** · `docs/prose-dialect-spec.md:118`

Nothing anywhere implements or tracks it. `grep -in slug` matches **zero files under
`crates/`** — no slugify algorithm, no fmt pass that writes `[slug]`, no collision
diagnostic; `brink-fmt` untouched. No issue owns it. Worse, **the spec still records
the pre-ruling model this decision replaced**: `docs/prose-dialect-spec.md:118-130`
§3.3 says an inferred slug leaves *"the title load-bearing… Retitling is a rename"*
with `@[was]` as the safety net, with no mention that tooling writes the slug on
format/save — because PR #1473 touched `docs/decision-log.md` only.

The rider is also shape-mismatched: the `[lints]` plane exists
(`ProjectConfig::lints`) but is keyed by raw diagnostic codes, so a named
`unmaterialized-slug` lint does not fit the current key shape.

## 23. XLIFF v1 excludes element data — element data is locale-invariant by construction

**2026-07-26** · `crates/internal/brink-intl/src/lib.rs:12`

Vacuously satisfied today (no element-data concept exists yet — `grep` for
`element_data`/`ElementData` over `*.rs` returns nothing), but **unowned and
unrecorded for the moment prose lands, which is exactly when the constraint bites**.
The only trace in main is a doc comment at `crates/internal/brink-intl/src/lib.rs:12-20`,
which records only the *deferral* half and not the v1 exclusion rule.
`docs/intl-spec.md` — the normative doc an implementer reads — never mentions element
data. `docs/prose-dialect-spec.md` states the adjacent half but never states "element
data is never exported in v1". No issue owns it; the optional lint has none either.

## 24. Host panel categories use nested `path`; value picker invoke is keyboard-reachable

**2026-07-15** · `packages/ink-editor/src/argument-widgets.ts:1025`

**Clause (1) is DELIVERED** (merged PR #270 — nested `path` in the manifest,
collapsible searchable sections). **Clause (2) was never implemented and nothing
tracks it.** This is the exact failure mode this audit hunts: the requirement was
added as a *comment* on **#211** (*"pressing ↑/↓ while the cursor is in the arg slot
opens the picker"*), and #211 was then **closed as COMPLETED** by PR #269 — whose
diff added no arrow-key handling. The only keymap in the argument-widget extension is
`Mod-Shift-A` (`argument-widgets.ts:1025-1030`); `openValuePicker` has exactly two
callers, both mouse/Enter-Space on the chip. Repo-wide `ArrowUp`/`ArrowDown` grep hits
only unrelated surfaces. Meanwhile `docs/argument-widget-spec.md:296-297` still states
the ↑/↓ requirement as shipped design.

## 25. Two-layer structural test macros

**2026-02-28** · `crates/internal/brink-syntax/src/parser/tests/cst.rs:32`

Half shipped, half dropped. `macro_rules! cst` exists at
`parser/tests/cst.rs:32` and is used widely. The `ast!` property-assertion macro was
never built **even though its stated precondition — the typed AST layer — has long
since landed**. `git grep 'macro_rules! ast'` matches only `ast_node!`; `git grep
'ast!('` matches nothing. AST tests hand-roll assertions through `parse_tree`/`first`
helpers instead. No issue owns it.

**Missing:** an `ast!`-style macro, or an explicit decision to retire the idea.

## 26. Dedicated template test corpus

**2026-03-14** · `crates/internal/brink-test-harness/Cargo.toml` (no `brink-intl` dependency)

No template/locale corpus tier exists. `git ls-tree origin/main:tests/` yields only
`dialect_fixtures`, `tests_github`, `tests_patched`, `tier1`, `tier1-brink`,
`tier1-brink-respell`, `tier1-native`, `tier2`, `tier3` — none intl/template.
`brink-test-harness` (the system the ruling named) has **no `brink-intl` dependency**
(verified: `grep -c brink-intl` = 0). No issue owns it — notable because #1529 *was*
filed and closed for the analogous native gap, and #1667 was filed for another
never-built 2026-03 ruling. What exists instead is scattered inline-source unit
coverage reusing an inklecate-derived tier1 case — exactly the ad-hoc substitute the
ruling said to replace.

## 27. User-configurable project scoping in LSP

**2026-03-16** · `crates/brink-lsp/src/backend.rs:911`

No user-facing knob to declare project roots, exclude files, or override include-tree
grouping. Grouping is still purely automatic. The two config surfaces are closed sets
and neither has a scoping key: `ConfigOverrides` (`backend.rs:911`, corrected from the
traced :916) = `dialect`/`types`/`lints`/`deny_warnings`; `ProjectConfig`
(`crates/internal/brink-project-config/src/lib.rs:192`) adds only `unprune_dirs`,
which can only *add* files. Nearest open issues #1572/#1580 concern native-`.brink`
project extent, not user-configurable `.ink` include-tree grouping. The failing case
named in the ruling's WHY (test-harness files INCLUDEing a subset) is still
unaddressed.

## 28. brink-studio standalone app uses Tauri

**2026-03-17** · `packages/brink-studio/package.json:2`; zero Tauri files in the tree

No desktop shell at all. Verified: `git ls-tree -r HEAD | grep -ic tauri` = **0** — no
`src-tauri/`, no `tauri.conf.json`, no dependency. What shipped is
`packages/brink-studio` as the embeddable npm library `@brink-lang/studio`
(`package.json:2`), per the later "npm publishing" entry — but that entry never
mentions a desktop distribution, so it does not supersede this ruling; the work just
went a different direction. No issue owns it.

**Secondary drift:** `docs/brink-studio-spec.md:837` still asserts *"The primary
standalone distribution is a **Tauri desktop app**"* while
`docs/studio-shell-spec.md:266` demotes it to "a future desktop shell" — the specs
disagree with each other and with reality.

## 29. Process: search the decision log before opening a design discussion

**2026-07-27** · `CLAUDE.md` (no match for "decision log"); `.claude/skills/autonomous-pump/SKILL.md:106`

No issue and no encoding anywhere a build agent will read it. `git grep -i
'decision.log' -- CLAUDE.md .claude` returns only `SKILL.md:88` and `:100`, both
describing the **backward-looking** ruling-ledger audit, not a forward gate before
design discussions. `CLAUDE.md` has no such rule (verified: `grep -in "decision.log"
CLAUDE.md` → none), despite the repo's own precedent of encoding standing process
rulings there. The companion citation rule exists only as `SKILL.md:106` — scoped
inside the ledger-audit workflow section, not a general house rule.
`BRINK-CONFIG.md:19` covers only the adjacent failure (decline needs-design issues).

**Missing:** a `CLAUDE.md` / `BRINK-CONFIG.md` rule requiring a decision-log + spec
grep with citation before any design proposal, and generalizing the no-uncited-citation
rule beyond the audit workflow.

---

# CONTRADICTED (13)

Main disagrees with the ruling as written. Each row cites the `file:line` showing the
disagreement.

## 1. Remove DIVERT_CHAIN node kind

**2026-02-28** · disagreement at `crates/internal/brink-syntax/src/parser/divert.rs:118` and `:121`

The kind *named* `DIVERT_CHAIN` is gone, but the substance is violated:
`divert_chain()` retroactively wraps the whole chain via
`p.start_node_at(checkpoint, TUNNEL_CALL_NODE)` (**:118**) or
`p.start_node_at(checkpoint, SIMPLE_DIVERT)` (**:121**), so `DIVERT_NODE` never
directly holds `DIVERT_TARGET_WITH_ARGS` — the snapshot shows
`DIVERT_NODE > SIMPLE_DIVERT > DIVERT_TARGET_WITH_ARGS`, i.e. the grouping wrapper the
ruling forbade, renamed. The AST layer derives tunnel semantics from wrapper kind, not
position; the only position rule lives a layer down in
`brink-ir/src/hir/lower/divert.rs`. History: `DIVERT_CHAIN` was removed in `78a46edc2`
and `SIMPLE_DIVERT` reintroduced the wrapper **44 minutes later** in `1fbc8502b`, while
the log entry only landed the next day — the ruling was already false the day it was
written, and nothing records that.

## 2. Merge MessageId and LineId into LineId scoped to containers

**2026-03-01** · disagreement at `crates/brink-runtime/src/output/mod.rs:40` and `crates/internal/brink-format/src/definition.rs:118`

Half landed (MessageId is gone), half is quietly violated with no issue tracking it.
**(1)** *"All user-visible text output uses LineId"* is false — `LineId` is a **dead
type**, occurring only in its own definition, one unit test, and a re-export. Actual
text output is addressed by `OutputPart::LineRef { container_idx, line_idx, .. }`
(`output/mod.rs:40`) and emitted by `Opcode::EmitLine(u16, u8)` — a bare scope-relative
index, no DefinitionId. `docs/runtime-spec.md:295` describes a `SpanStart(LineId)`
output part that does not exist. **(2)** *"Each container carries its own line
sub-table"* is false — the sub-table is per lexical **scope**, explicitly shared: `pub
struct ScopeLineTable` (`definition.rs:118`) is documented *"All containers within a
lexical scope (knot, stitch, or root) share one ScopeLineTable."* That second change was
propagated into `docs/format-spec.md` but never back into the log.

## 3. Choice text: dual-path bytecode model

**2026-03-02** · disagreement at `crates/brink-runtime/src/vm.rs:2427`

Both paths exist, but the ruling's load-bearing invariant does not. The ruling says
`BeginChoice` *"always pops display text off the value stack regardless of path"*.
`vm.rs:2427` reads `let has_display = flags.has_start_content ||
flags.has_choice_only_content;` and pops **only when `has_display` is true**;
`opcode.rs:747-753` documents this deliberate "single-pop protocol". Concrete
divergence: `codegen-inkb/src/container.rs:368-374` (the tags-only-choice branch) emits
`BeginStringEval`/`EndStringEval`, `EndStringEval` pushes a `Value::String`, yet both
content flags are false there — so `BeginChoice` never pops it and `EndChoice` only
clears a bool. An **orphaned value-stack entry** that the "always pops" rule was written
to prevent. Worth a targeted test on a choice with tags and no display text.

## 4. Compiler is I/O-agnostic, file reading injected by caller

**2026-03-05** · disagreement at `crates/brink-compiler/src/driver.rs:68`

Honoured on the `.ink` path (`compile(entry, read_file)` takes an injected closure).
Contradicted on the newer native `.brink` surface: `driver.rs:68` dispatches on the
entry extension and, for a native entry, constructs `RealFs` and calls
`driver.discover_native(&tree)?` — **real filesystem I/O inside the core compile entry
point, with the caller's `read_file` silently unused**. The code says so itself
(`driver.rs:49-54`). A WASM/editor/test caller compiling a `.brink` entry through
`compile()` cannot supply buffers.

*Tracked:* **open issue #1306** explicitly owns reconciling this, so the fix has a home
even though the current code disagrees.

## 5. Built-in function recognition belongs in the analyzer

**2026-03-06** · disagreement at `crates/internal/brink-analyzer/src/resolve.rs:855` vs `crates/internal/brink-ir/src/lir/lower/expr.rs:2033`

Two of three clauses delivered; the third is quietly violated. The ruling says *"The set
of built-ins is defined in brink-ir (shared between analyzer and LIR) so both layers
agree."* brink-ir exports **no** public builtin-name predicate — `recognize_builtin` is a
private `fn` in a private lowering module (`expr.rs:2033`). The analyzer therefore keeps
a full hand-maintained duplicate (`resolve.rs:855`, `pub(crate) fn is_builtin_function`).
Not a dependency problem: `brink-analyzer/Cargo.toml` already declares `brink-ir`. **The
code knows it is violating the ruling and says so** in doc comments on both sides. The
duplication has since propagated to a second pair (`is_t1b_stdlib_name`) and already
required a defensive runtime diagnostic (E144) for when the copies diverge. The two
lists currently agree name-for-name, so the contradiction is structural, not yet a live
bug. No issue owns it.

## 6. Use thiserror for error types

**2026-03-14** · disagreement at `crates/internal/brink-codegen-inkb/src/lib.rs:44`, `crates/internal/brink-format/src/inkt/read.rs:34`, `crates/internal/brink-format/src/opcode.rs:887`

The "new crates must use it" half is honoured (30 files derive `thiserror::Error`). The
"existing crates migrated when touched" half is violated in two crates touched
constantly since: `impl std::fmt::Display for CodegenError` (`codegen-inkb/src/lib.rs:44`),
`impl core::fmt::Display for InktParseError` (`inkt/read.rs:34`), `impl fmt::Display for
DecodeError` (`opcode.rs:887`) — all hand-rolled, and **neither crate even carries a
thiserror dependency**. Those files received 21 / 43 / 40 commits since the ruling, so
"touched" is not in doubt. No issue owns the migration.

## 7. CLI intl commands speak XLIFF only

**2026-03-14** · disagreement at `docs/intl-spec.md:174` and `:302`

Split result. **(a) The CLI half IS delivered** — brink-cli's only localization
subcommands are `ExportXliff`, `CompileLocale`, `RegenerateXliff`, `MigrateXliff`; there
is no JSON-lines subcommand. **(b) The "LinesJson is internal, never user-facing" half is
contradicted in the spec**: `docs/intl-spec.md:174` still calls the JSON file *"The
primary export format"*, and `:302` heads a section *"### Translation workflow (JSON)"*
whose text tells translators to author a translated `lines.json`. The architecture
diagram shows export/compile `lines.json` as first-class pipeline arms. No issue owns
this drift.

## 8. Screenplay keybinding transitions

**2026-03-21** · disagreement at `packages/ink-editor/src/keybindings.ts:357` and `packages/ink-editor/src/transitions.ts:191`

Three of the ruled table's rows are quietly violated. The element picker exists but is
reachable **only via Alt+Enter** — `keybindings.ts:357` is `{ key: "Alt-Enter", run:
showInlineElementPicker }`, the only picker binding — while the ruling's whole rationale
("Element picker on blank Enter avoids the need for a toolbar") depends on plain Enter.
Empty-dialogue Enter strips to narrative instead of offering the picker
(`transitions.ts:191`); dialogue-with-text Enter continues the chain instead of exiting
to action; the Shift+Enter stay-in-dialogue affordance was never added. The divergence
dates from the same day as the ruling and **the spec was never reconciled** —
`docs/brink-studio-spec.md:233/:235/:239` still assert the ruled behaviour. May be
overtaken by open #1351, but is not superseded today.

## 9. Track B MVP implementation decisions (check scope, severity flag, data placement)

**2026-06-09** · disagreement at `crates/internal/brink-analyzer/src/external_check.rs:36` and `crates/brink-web/src/editor/mod.rs:479`

Three of four sub-commitments hold. The ruled severity set `{Error (default), Warning,
Off}` is **not** what shipped: `pub enum ExternalCheckSeverity` (`external_check.rs:36`)
has only `Error`/`Off`, and the wasm setter (`editor/mod.rs:479`,
`set_external_check(&mut self, level: &str)`) hard-errors on `"warning"`. PR #74's body
listed "Warning severity level" under Deferred (reason: `Diagnostic` carries static
per-code severity), but **no issue was ever filed** — so the missing tier is itself
orphaned.

## 10. npm publishing: @brink-lang scope, two packages (web + studio)

**2026-06-11** · disagreement at `packages/ink-editor/package.json:2` vs `docs/publishing.md:3`

The mechanism shipped, but the ruling's load-bearing clause — *exactly two packages*,
with `ink-editor` bundled and staying private — is violated. Merged **PR #346**
un-privated `packages/ink-editor`, renamed it `@brink-lang/editor` (`package.json:2`) and
gave it `publishConfig.access: public`. There are now **three** published `@brink-lang`
packages, in lockstep at 0.14.0. **No decision-log entry authorizes this** — grep for
"@brink-lang" / "publish" / "promote" / "editor package" finds no superseding ruling. The
contradiction is live in the docs too: `docs/publishing.md:3` still says *"Two packages
publish to npm"* and `:11` says everything else *"stays `private: true` and is never
published"*; the workflow header comment still names only web+studio. The other three
internals do still carry `private: true`.

**Recommended:** either log the promotion as a superseding decision and fix
`docs/publishing.md` + the workflow comment, or re-privatize.

## 11. Track A synthetic-container identity + full-branch rails

**2026-07-07** · disagreement at `crates/internal/brink-ide/src/hir_projection.rs:385` vs `docs/editor-hir-overlay-spec.md:288`

The ruling says synthetic-container identity is **range-derived** and that *"the handle is
the full range (or `(start, kind, depth)`), not a bare start-offset, to avoid
same-block false-matches."* The code assigns a monotonically-incrementing **walk-order
ordinal** instead (`hir_projection.rs:385` = `let handle = self.next_handle;`). The spec
was never updated: `docs/editor-hir-overlay-spec.md:288` still annotates the field as
*"stable-within-doc id (range-derived)"*, so the spec now misdescribes the
implementation. Effect today is benign, but it is strictly **less stable** than a range
handle — inserting any earlier container renumbers every subsequent handle, and
`Projection.option_paths` is keyed by that handle. The rest of the ruling is delivered.

**Fix is a one-line choice:** derive the handle from the range as ruled, or amend the
spec to record walk-order as the accepted mechanism.

## 12. T1c spec rulings (draft round) — item (4): effect-row field ships from day one on every fn value

**2026-07-13** · disagreement at `crates/internal/brink-format/src/value.rs:421` and `crates/internal/brink-analyzer/src/infer/ty.rs:58`

No effect-row field exists on a function value anywhere: `pub struct ClosureValue`
(`value.rs:421`) is `{ target, env }`; `Ty::Fn` (`infer/ty.rs:58`) is `Fn(Vec<Ty>,
Box<Ty>)` with no row; the wire form encodes only target+env with no reserved row slot.
Rows instead live in a separate DefinitionId→row `EffectRows` section, and a call through
a fn value falls to `EffectRow::pessimal`. So the pessimal *semantics* hold while the
ruled *field* never shipped. **`docs/t1c-spec.md:166` still asserts** *"The effect-row
field exists on every function value from creation"* — stale spec a future agent will
implement against. The same-day "T2 effects rulings" entry re-ruled the row onto `Ty::Fn`,
which is also absent, so the doc/code disagreement is live in both places.

## 13. Maps ruled: contains_key/contains_value, get-Option, insert reserved, lambda debt recorded

**2026-07-18** · disagreement at `crates/brink-runtime/src/collection_ops.rs:257`

Two named commitments are quietly violated, neither with an issue. **(1)** `insert` was
ruled RESERVED-not-shipped and re-affirmed 2026-07-19; `docs/stdlib-spec.md:298` says *"No
`insert` verb ships."* But `insert` **is** a live map verb: `pub(crate) fn map_insert`
(`collection_ops.rs:257`) has a `ValueType::Map` branch; the analyzer narrows
`Ty::Map(k, v)` on it; it is in `is_t1b_stdlib_name`, lowers via `MutatorKind::Insert`,
emits `Opcode::MapInsert`, and is regression-tested as a map verb.
`docs/stdlib-inventory.md:295` papers over this by claiming the dispatched `insert` is the
array positional insert — only half true; the same name still inserts by key into a map.
**(2)** `contains_key` was ruled as the map membership spelling; it is **not dispatched
anywhere** (`git grep contains_key -- crates` hits only a doc comment and an unrelated
`OrderedMap` helper). Map membership is still polymorphic `contains`, and #582 (CLOSED)
built the E152 key-domain warning **on that spelling**, entrenching it. The rest of the
ruling is delivered (7/8 verbs). No open issue owns either respell.

---

# TRACKED (22)

Not built, but an open issue owns them. Correctly parked.

| Title | Date | Verdict | Evidence |
|---|---|---|---|
| Runtime holds unlinked + linked layers; patching re-links | 2026-03-01 | TRACKED | issue #848 OPEN; umbrella #1093 OPEN. No unlinked layer exists; `brink-runtime/src/linker.rs:33` is whole-program `link` only. Mechanism redirected to splice-not-re-link by the 2026-07-15 FG entry. |
| Editable runtime state (save/restore variables) is a follow-up | 2026-06-04 | TRACKED | issue #57 OPEN (Studio QoL). Wasm setter already shipped (`StoryRunner::set_var`); only the studio UI + take-effect UX remain. Note `BRINK-CONFIG.md:22` parks studio work, so effectively stalled. |
| Studio QoL milestone — the #115 host-usage/reachability requirement | 2026-06-14 | TRACKED | issue #115 OPEN. `HostManifest` (`brink-ir/src/host_manifest.rs:36`) has only `externals`/`types` — no entry-knot declarations. **Warning:** the shipped CLI `unused` is reference-based, not reachability, and exits 1 — if it ever feeds studio warnings or CI gating before #115 lands, this becomes a live violation. |
| Metadata-tag APIs: static tags in the format, dynamic via speculation | 2026-07-10 | TRACKED | issue #474 OPEN. `git grep -iE 'global_tags\|globalTags\|tags_for_content'` returns exactly one hit — the decision-log entry itself. Open since 2026-07-10 with zero movement while everything else in that day's batch shipped within ~24h. |
| FG epic closes on the honest reanalyze win; incremental assembly deferred post-T2 | 2026-07-15 | TRACKED | issues #848 + #1093 both OPEN. No splice path exists; `linker.rs:33` is whole-program only. #717 closed by folding into #1093, not by shipping — if #1093 closes without #848, this becomes ORPHANED. |
| FlowFrame suspension + await: designed and parked | 2026-07-16 | TRACKED | issues #889, #928, #980 OPEN. Format section + `await` grammar landed; runtime spill/restore not — `brink-runtime/src/story/types.rs:53-55` states park/spill/resume "do not yet exist". `await` partly superseded by `until` on the native surface. |
| First-class FSMs & entity statecharts: chartered for T3/4 | 2026-07-17 | TRACKED | issue #905 OPEN. No `scxml`/`statechart` code anywhere in `crates/`. Boundaries actively honoured as constraints in `docs/hir-admission-contract.md:36-40` and `:597-599`. |
| FS-3 design round: flow-addressed wake API, parked-flow save/load, caps | 2026-07-18 | TRACKED | issue #980 OPEN, epic #889 OPEN. Interface half shipped (`Line::Suspended`, `Story::wake_check`); behaviour deliberately fenced — `story/mod.rs:1538-1589` asserts the runtime must never construct `Line::Suspended` before FS-3r. |
| FS-3 implementation: continuation-splitting, invisible containers, web-first slicing | 2026-07-18 | TRACKED | issue #980 OPEN. Reserved surface only: `hir/frame_shape.rs:56` `ContinuationSite`, `format/counting.rs:28` `INVISIBLE`. Nothing emits it — `codegen-inkb/src/lib.rs:153-157` hard-codes `frame_shapes: Vec::new()` behind the E052 fence. |
| Flags ruled: the LIST-op audit lands; the numeric coupling is frozen | 2026-07-18 | TRACKED | issue #1541 OPEN. `docs/stdlib-inventory.md:317`: "Built: none of the native verbs." Freeze half satisfied by construction. **Caveat:** #1541 does not name `next`/`prev`/`range`/`invert`/`index_of`. |
| Unified block/effect/coroutine model ratified (native surface) | 2026-07-20 | TRACKED | issue #1665 OPEN — **created 2026-07-27, nine hours before this audit ran**; it was orphaned until then. `docs/block-effect-model.md:3`: "Design doc; not yet implemented." Sub-item (4) partly superseded by the 2026-07-20 scoping correction. |
| Modular-artifacts round convened: load-time mounting, layered-precedence resolver | 2026-07-23 | TRACKED | issue #1093 OPEN. Design-only as intended. `linker.rs:33` takes a single `&StoryData` → one flat map; no mount API, no layer stack, no `#@override`. The ~5 deferred surfaces have no separate issues — if #1093 closes without them they orphan. |
| Direction (tentative): flows-as-actors — cross-flow invocation is a message-send | 2026-07-23 | TRACKED | issue #1210 OPEN (needs-design); #597 CLOSED 2026-07-26 with a comment consolidating into #1210, exactly as directed. No homing/message-send code. |
| Choice-guard `as` un-deferred: capture-at-presentation, by-value (COW), rides v6 | 2026-07-26 | TRACKED | issue #1508 OPEN, blocked on the v6 bump. Interim commitment delivered: E146 raised at `brink-ir/src/hir/lower_native/choice.rs:117-123`, documented as "not yet", not "not a thing". **Staleness:** #1508's body calls it E141; the code emits E146. |
| Three surface rulings: bracket array literals, dual-reading `use`, annotation-first global typing | 2026-07-27 | TRACKED | issues #1490, #1592, #1549 all OPEN; ruling recorded by merged PR #1656. `[` is lexed but the native expression parser has no bracket arm; `use` checks handle only the bare-item form. No contradiction found. |
| Frame-local projection receivers are legal; effect rows are a durable-cell concern | 2026-07-27 | TRACKED | issue #1531 OPEN; recorded by merged PR #1660. **One issue-close from becoming a live contradiction:** `brink-analyzer/src/ufcs.rs:727` still pushes E143, and an active test (`tests/ufcs_resolution.rs:597-620`) pins the refusal the ruling overturns. That test must be inverted when #1531 lands. |
| Flags verbs live in the type's companion module (#1541) | 2026-07-27 | TRACKED | issue #1541 OPEN. **Spec not updated** — PR #1663 touched `docs/decision-log.md` only, so `docs/stdlib-inventory.md:336` still carries the rejected spelling. Its prerequisite (companion-module machinery) is ORPHANED — see ORPHANED finding 9. |
| Supersession notes: angle brackets over `Option[T]`; casing partition required UpperCamel | 2026-07-27 | TRACKED | issue #1552 OPEN. Entry landed via merged **PR #1663** (`0d95ec3ea`) — **not PR #1659, which is still OPEN.** `brink-analyzer/src/annotations.rs:153-166` still uses the lowercase vocabulary; `Option<T>` remains unannotatable. |
| Format durability doctrine: strictness follows durability; sections skippable; `.brkt` is a log | 2026-07-27 | TRACKED | issue #1519 OPEN. `format/inkb/mod.rs:312-314` `SectionEntry` has **no length**; the two positional `.brkt` probes the ruling orders retired are still live at `brink-runtime/src/transcript.rs:308` and `:333`. Note `docs/implementation-roadmap.md`, referenced by #1519, does not exist at this ref. |
| R1 (G1): compiler must emit an alias per descendant on a declared rename | 2026-07-27 | TRACKED | issue #1671 OPEN. `brink-intl/tests/rename_identity.rs:156` pins `alias_table.len() == 1` — the test asserts today's non-transitive behaviour. |
| R1 (G2/G3): IDE must write `#@was`; undeclared renames detected by manifest diff | 2026-07-27 | TRACKED | issue #1672 OPEN. Code side asserted from the issue's own "never implemented" framing; the IDE/LSP rename path was not exhaustively audited. |
| R1 (b)+(d): LoadReport surfaces dropped anonymous state; lint for unnamed stateful choices | 2026-07-27 | TRACKED | issue #1674 OPEN. `brink-runtime/src/save.rs:249-268` only reports unresolved renames for entries carrying an author `path`, so anonymous entries drop silently. No unnamed-stateful lint. |

---

# DELIVERED (203)

The commitment is in the tree. For process rulings, "delivered" means the rule is
binding in the repo's instruction surfaces and demonstrably invoked.

| Title | Date | Verdict | Evidence |
|---|---|---|---|
| Wrap bare identifier tokens in IDENTIFIER nodes | 2026-02-28 | DELIVERED | `brink-syntax/src/parser/knot.rs:59` (`p.start_node(IDENTIFIER)`), plus every site the ruling named; accessors on `ExternalDecl`/`KnotHeader`/etc. Direct-commit delivery, no PR. |
| Remove PIPE_PIPE compound token from lexer | 2026-03-01 | DELIVERED | `brink-syntax/src/lexer/punctuation.rs:88` (`b'\|' => (PIPE, 1)`); parser rebuilds OR from two PIPEs; `PIPE_PIPE` appears nowhere but the log. |
| Bytecode VM, hot-reload, and multi-instance are core requirements | 2026-03-01 | DELIVERED | `brink-runtime/src/story/mod.rs:114` (`Story::new(program: Arc<Program>, …)`); `linker.rs:33`; `format/definition.rs:372` `AddressDef`; `bevy-brink/src/replay.rs:132`. "KnotId" never shipped (DefinitionId replaced it); reconciliation is rebuild-and-replay, granular splice owned by #848. |
| HIR lowering and semantic analysis are separate crates | 2026-03-01 | DELIVERED | `brink-ir/src/hir/lower/structure/mod.rs:33` `pub fn lower`; `brink-analyzer/src/lib.rs:437` `pub fn analyze`. Only the crate name changed (brink-hir → brink-ir). |
| brink-format firewall between compiler and runtime | 2026-03-01 | DELIVERED | `brink-runtime/Cargo.toml` lists exactly one brink crate (`brink-format`); no_std proven by feature flags + merged PR #514. Caveats: `brink-compiler` is a dev-dependency; brink-format is published despite living in `crates/internal`. |
| Containers are the fundamental compilation and runtime unit | 2026-03-01 | DELIVERED | `brink-codegen-inkb/src/lib.rs:504` (`state.chunks.push(ContainerChunk…)`); `brink-ir/src/lir/types.rs:209` `Container`; `brink-runtime/src/vm.rs:2314` `goto_target`. Docs disagree: `containers.md` describes an inlining path codegen does not take. |
| Bytecode uses ContainerIds (path hashes), resolved at load via linker | 2026-03-01 | DELIVERED | `brink-runtime/src/linker.rs:33`; `format/opcode.rs:1017` `Goto(DefinitionId)`. Renamed to DefinitionId. Caveats: `linker.rs:99-103` still relies on a positional root convention; the hot-reload-patching clause is not delivered (#848). |
| Plural resolution delegated to host with batteries-included option | 2026-03-01 | DELIVERED | `format/src/line.rs:114` `pub trait PluralResolver`; `brink-intl/src/plural.rs:11` `IcuPluralResolver`. Trait lives in brink-format, not brink-runtime; the locale-pruning clause was consciously dropped with rationale in `intl-spec.md:547`. |
| XLIFF 2.0 as the localization authoring format | 2026-03-01 | DELIVERED | `brink-cli/src/main.rs:123` `ExportXliff`; `brink-intl/src/xliff_convert.rs:38`. Deviations: attribute is `brink:hash` not `brink:contentHash`; subcommand is `export-xliff`; the `translations/<locale>.xlf` convention is nowhere enforced. |
| Uniform DefinitionId with tagged type discriminant | 2026-03-01 | DELIVERED | `format/src/id.rs:52` `pub struct DefinitionId(u64)`, HASH_MASK 56-bit, `DefinitionTag` covers all five ruled kinds + two added since. `StringId`/`MessageId` clause is stale — neither type exists. |
| brink-hir exports composable transforms, not a monolithic pass | 2026-03-02 | DELIVERED | `brink-ir/src/hir/mod.rs:26` re-exports `fold_weave`, `lower`, `lower_single_knot`, `lower_top_level`. **But** `fold_weave` has zero callers outside brink-ir, and `docs/compiler-spec.md:52`/`:69` assert a merged-content model that the INCLUDE-root-terminus changeset reversed — that spec text is now false. |
| Pluggable PRNG for runtime | 2026-03-03 | DELIVERED | `brink-runtime/src/rng.rs:13` `pub trait StoryRng`; `FastRng` default via `Story<R: StoryRng = FastRng>`; `DotNetRng` used for reference fidelity. Pluggability is live, not vestigial. |
| Layered execution model: dumb VM, smart orchestration | 2026-03-04 | DELIVERED | `brink-runtime/src/vm.rs:34` `pub(crate) enum Stepped` with `ThreadCompleted`/`ExternalCall`; four layers present. Glue-lookahead sub-clause could not be verified — Glue is an output-buffer push, no lookahead found. |
| Instanced flows with per-entity contexts | 2026-03-04 | DELIVERED | `brink-runtime/src/story/mod.rs:782` `spawn_flow` → `(FlowInstance, World, FlowLocal)`; entity→instance mapping lives one layer up as `BrinkFlow<M>`, per the bevy-free ruling. |
| External function handler is a trait, not a binding map | 2026-03-04 | DELIVERED | `brink-runtime/src/story/external.rs:28` `pub trait ExternalFnHandler`; three-variant `ExternalResult`; passed as `&dyn`, not stored on Story. Two downstream policies implement it. |
| HIR is a rich semantic tree, not a thin bytecode-adjacent IR | 2026-03-04 | DELIVERED | `brink-ir/src/hir/mod.rs:1` restates the ruling as module doc; `hir/types.rs` keeps expression trees, semantic diverts, branch structure, attached tags. |
| Per-knot incremental HIR lowering boundary | 2026-03-04 | DELIVERED | `brink-ir/src/hir/lower/mod.rs:51` exports `lower_single_knot`/`lower_top_level`; brink-db consumes them. **Not built as ruled:** green-node-identity caching — HIR lowering is memoized per *file*; per-def memoization lives downstream (PR #837). No open issue owns HIR-level per-knot avoidance. |
| brink-ir owns all symbol types | 2026-03-05 | DELIVERED | `brink-ir/src/symbols/index.rs:19` `SymbolIndex` + `SymbolInfo`/`SymbolKind`/`Scope`/`SymbolManifest`; no ir→analyzer cycle. Erosion: the analyzer now defines a substantial type surface of its own (`Ty`, `TowerTy`, …), so the "no types of its own" clause is no longer true. |
| Flat Exxx diagnostic codes | 2026-03-05 | DELIVERED | `brink-ir/src/hir/diagnostics.rs:61` `pub enum DiagnosticCode`; E001–E155 docs, no W-prefix namespace, retired codes reserved; coverage machine-enforced both ways. **Unbuilt sub-item:** `--explain` does not exist and no issue owns it. |
| Positions use resolved indices at runtime | 2026-03-05 | DELIVERED | `brink-runtime/src/story/call_stack.rs:17` `ContainerPosition { container_idx: u32, offset: usize }`. Caveat: divert *operands* still resolve by hash lookup per-opcode, so the ruling's stated rationale does not hold on the divert path. `story.reload` does not exist. |
| Flag silent data drops | 2026-03-06 | DELIVERED | `CLAUDE.md:156` carries the rule; invoked by name as a decision criterion in later log entries (E058, F2 rename). Convention, not mechanical — exactly as ruled. |
| LIR container tree with ID-based lookup | 2026-03-06 | DELIVERED | `brink-ir/src/lir/types.rs:11` `Program { root: Container }`, `:209` `Container { children }`; choice bodies/gathers by DefinitionId; no `Container` inside any `Stmt`. The O(1) `HashMap` lookup was never built — benign, since the JSON backend that motivated it was retired. |
| RCA-first work loop with failing tests | 2026-03-10 | DELIVERED | `CLAUDE.md:41-51` + `:58` encode all four elements verbatim. Two later rulings erode its reach without repealing it (conformance deprioritized; pump waves); CLAUDE.md still reads as though plan mode gates every compiler fix. |
| compile-locale requires .inkb as base input | 2026-03-13 | DELIVERED | `brink-intl/src/compile.rs:18/27/88`; magic-byte rejection at `format/inkb/read.rs:92-101`; checksum enforced at `brink-runtime/src/locale.rs:33`. `.ink.json` now rejected outright. No negative test for a non-.inkb base. |
| General-purpose XLIFF 2.0 crate + brink-intl separation | 2026-03-13 | DELIVERED | `crates/internal/xliff2` depends only on quick-xml + thiserror; only "brink" strings are inside its own `#[cfg(test)]`. `BRINK_NS` and all brink concerns sit in brink-intl. |
| Break opcode format for slot count | 2026-03-14 | DELIVERED | `format/src/opcode.rs:1048` `EmitLine(u16, u8)` + `:1056` `EvalLine(u16, u8)`; threaded end-to-end through encode/decode, .inkt, disasm, proptests, codegen and VM. One stale doc line at `definition.rs:116`. |
| Combine interpolation recognizers | 2026-03-14 | DELIVERED | `brink-ir/src/lir/lower/recognize.rs:81` `try_recognize` — one general template arm looping over every part, no single-slot phase, no upper bound. `docs/intl-spec.md:137` agrees. |
| Metadata fields as stubs | 2026-03-14 | DELIVERED | `format/src/definition.rs:86-87` `slot_info` + `source_location` on `LineEntry`; serialized both ways in .inkb and .inkt. Now populated (commit `ef02aebb9`) with no format break — which is what the ruling was buying. |
| Select defaults to fallback | 2026-03-14 | DELIVERED | `brink-runtime/src/output/mod.rs:288` — bare `default` as the terminal arm of `resolve_select`. Phase 6 landed on top. Caveat: `resolve_select` is duplicated verbatim at `vm.rs:2111-2182`. |
| Regeneration uses hash-based alignment, not index matching | 2026-03-14 | DELIVERED | `brink-intl/src/align.rs:18` `pub fn align_hashes` (LCS DP + backtrack), consumed at `regenerate.rs:60`. All four clauses hold; XLIFF path shares the code. Unverified lead: `slots: Vec::new()` on regenerated lines may lose slot naming. |
| xliff2 crate naming | 2026-03-14 | DELIVERED | `crates/internal/xliff2/Cargo.toml:2` `name = "xliff2"`; `lib.rs:16` XLIFF 2.0 NS; only "1.2" occurrence is a negative validation test. Publishable per repo config (not verified against crates.io itself). |
| xliff2 module architecture | 2026-03-14 | DELIVERED | `xliff2/src/model/extensions.rs:6` generic namespace-qualified bag; `modules/mod.rs:1` metadata behind a feature; typed accessors over the bag, not new core fields. Merged PR #1628 enabled the feature in-workspace. |
| Source filename missing from StoryData — spec gap | 2026-03-14 | DELIVERED | `format/definition.rs:71-77` `SourceLocation { file, … }` on `LineEntry`, populated from `ctx.file_paths`, serialized in .inkb. **But the stated purpose is orphaned:** XLIFF `<file original>` is hardcoded `None` at `brink-intl/src/xliff_convert.rs:40` and no issue owns that last hop. |
| Extricate line_tables from Program | 2026-03-14 | DELIVERED | `brink-runtime/src/program.rs:15` `Program` has no `line_tables`; `link()` returns the tuple; `Story` owns them with `set_line_tables`; `apply_locale` takes `&Program` and returns fresh tables. Stale doc nit at `program.rs:96`. |
| Branch expansion for inline sequences/conditionals in the content recognizer | 2026-03-15 | DELIVERED | `brink-ir/src/hir/normalize.rs:20` `normalize_file` (lift inline to block level), cartesian recursion, per-branch recognition, EmitContent fallback. **Scoped gap documented only in a test:** normalization never touches a Choice's own display text, so choice text is under-covered for translation counting. |
| #voice tags are not real | 2026-03-15 | DELIVERED | `docs/intl-spec.md:103` states `audio_ref` is populated by external tooling, not the compiler; `#voice` appears only in the log. `brink-codegen-inkb/src/lib.rs:322` — both LineEntry construction sites write `audio_ref: None`. |
| Fountain-inspired context-dependent element transitions | 2026-03-18 | DELIVERED | `packages/ink-editor/src/transitions.ts:140` `context?: TransitionContext`, honoured at `:221`; wired in `keybindings.ts:156-157`. **Caveat:** zero rows in the shipped table set `context`; the practical work migrated to dialect `ChainRule` (merged PR #386). *(Traced twice; same finding.)* |
| EditorSession with cached parse/HIR as single source of truth | 2026-03-18 | DELIVERED | `brink-web/src/editor/mod.rs:50` `pub struct EditorSession`; `editor/spans.rs:17` `line_contexts`; `brink-ir/src/hir/types.rs:643` `depth` on ChoiceSet. **Unmet sub-clause:** the regex `classifyLine` still exists and runs as a fallback (`element-type.ts:217`, called at `:608`/`:636`); re-scoped by closed #480, no open issue finishes it. *(Traced twice.)* |
| FileProvider: async host-owned file I/O interface | 2026-03-19 | DELIVERED | `packages/ink-editor/src/provider.ts:8` `export interface FileProvider` — all six ruled methods present and consumed by `project-session.ts`. Synchronous `compileProject(): CompileResult` holds. Three methods are optional rather than required; interface has grown additively. |
| View context as native wasm concept | 2026-03-19 | DELIVERED | `brink-web/src/editor/mod.rs:526` `set_view_context(&mut self, start, end)`; splice in wasm, IDE responses filtered + rebased, TS calls through. Signature deviation: the path-parameterized form landed later as `open_fragment(path, start, end)`. |
| Screenplay conventions (character/dialogue/parenthetical) | 2026-03-21 | DELIVERED | `brink-ir/src/dialect.rs:849` `at_cue_preset()`; sigil geometry + full cursor-trap state machine in `packages/ink-editor/src/{screenplay,keybindings}.ts`. **Orphaned sub-commitment:** character-name autocomplete was never built and no issue owns it; re-ruled at a different altitude under open #1351. |
| Binder multi-select: same-kind siblings only | 2026-03-22 | DELIVERED | `packages/studio-store/src/slices/binder.ts:80` `parseKey`, `:87` `areSameKindSiblings`, `:117` `selectKey`. Enforced structurally — `selectKey` is the sole writer, so a mixed selection is unreachable. Untested invariant; folder rows may classify as file-kind siblings. |
| Binder structural moves use store-level undo | 2026-03-22 | DELIVERED | `binder.ts:185` `applyMoveResult` pushes an edits entry; `:278` `async undo()`; Ctrl/Cmd+Z at `Binder.tsx:705`. Snapshots include `cross_file_edits` paths — the cross-file reversal rationale. |
| Binder multi-select via Ctrl/Cmd + click | 2026-03-22 | DELIVERED | `packages/studio-ui/src/Binder.tsx:521` (`isMulti`, returns without opening a tab); plain click preserves click-to-open. No Shift+click range selection, as ruled for V1. |
| Fragment model for locale-safe slot values | 2026-03-23 | DELIVERED | `brink-runtime/src/output/fragment.rs:21` `Fragment { parts, tags }`; `format/opcode.rs:1058/1060` Begin/EndFragment; `Value::FragmentRef`; merged PR #1308. Shape refined (tags added); emission site moved to whole lines. **Stale doc:** `codegen-inkb/src/expr.rs:9-11` still claims display-flag wrapping; the param is `_display`. |
| Replace converter pipeline with C# ink oracle as correctness source | 2026-03-23 | DELIVERED | Commit `24a1027b0`; verified at this ref: **zero** `episode.json` paths in the tree, **6,639** `*.oracle.json` files. The escape hatch is permanently foreclosed by the later converter retirement. CLAUDE.md's crate table is stale against this. |
| HIR lowering: split context into read-only scope + write-only sink | 2026-03-28 | DELIVERED | `brink-ir/src/hir/lower/context.rs:23` `LowerScope` + `LowerSink`; `&mut LowerScope` appears only in `structure/{knot,stitch}.rs`. Commit `ad9f7162a`. The sink's symbol surface was later removed (narrowed, still write-only); `lower_native` does not use the discipline. |
| Eliminate LIR planning pass via upstream structural IDs | 2026-03-29 | DELIVERED | `brink-ir/src/lir/lower/mod.rs:432` `hir::stamp_container_ids(…)`; `hir/stamp.rs` exists and says it replaces the planning pass; no planner module survives. Placement caveat: stamping runs inside the LIR prelude, not at the end of HIR lowering. |
| bevy-brink loading modes | 2026-04-24 | DELIVERED | `bevy-brink/src/plugin.rs:460` `init_asset_loader::<InkbLoader>()` (+ `InklLoader`), dev-mode `InkLoader` behind `#[cfg(feature = "dev")]`; compiler is an optional dep, so `--no-default-features` really drops it. Merged PR #1362. Stale comment at `asset.rs:61`. |
| Marker-parameterized bevy types | 2026-04-24 | DELIVERED | `bevy-brink/src/{plugin,globals,flow}.rs` — `BrinkPlugin<M = ()>`, `BrinkGlobals<M = ()>`, `BrinkFlow<M = ()>`. `BrinkLineTables` no longer exists; its role is `BrinkCurrentLocale<M>` + `BrinkLocale<M>`. Documented marker-free exceptions with a cost the crate itself calls out. |
| brink-runtime stays bevy-free | 2026-04-24 | DELIVERED | `brink-runtime/Cargo.toml` has no bevy dep; zero bevy imports in any source file. All Resource/Component derives in bevy-brink. **Caveat:** the "every public type is Send+Sync+'static" clause has no static assertion — upheld only implicitly. |
| ink↔engine binding boundary: split by World access, not sync/async return shape | 2026-05-29 | DELIVERED | `bevy-brink/src/plugin.rs:343-346` gated `resolve_pending_externals`; `bindings/drive.rs:809` `run_system_with` then `resolve_external`; per-call-entity `BrinkCallResolved<M>`. Merged PR #1645. |
| Engine↔ink dynamic binding facility: resumable eval + exclusive driver | 2026-05-31 | DELIVERED | `brink-runtime/src/story/flow_instance.rs:872`/`:1048`; `FunctionEval` enum; `CallFrameType::FunctionEvalFromGame`; `bind_brink_query`. Naming delta: the ruled `on_resolved` does not exist — shipped as `.observe(…)`; no log entry records the rename. |
| bevy-brink locale switching: global + event-driven | 2026-05-31 | DELIVERED | `bevy-brink/src/locale.rs:127` `BrinkCurrentLocale<M>`, `:143` `BrinkLocaleChanged<M>`, `:187` cache keyed by (base, locale) AssetIds. All three paths funnel through one helper — no polling. Override opt-out enforced structurally by `Without<…>`. |
| bevy-brink async bindings: event primitive + task sugar, flow-entity correlation | 2026-06-02 | DELIVERED | `bevy-brink/src/bindings/registration.rs:444`/`:464`; `async_bind.rs:55/103/139/204`. Rejection clause honoured on **both** one-pass drivers (`drive.rs:206` and `:628`), with regression coverage. |
| Book playground WASM built in CI, not committed | 2026-06-02 | DELIVERED | Merged PR #1; `justfile:118` `book-assets`; `.gitignore:4`/`:8`; `.github/workflows/book.yml` installs wasm-pack. Verified nothing committed. Amended (not superseded) by the 2026-06-04 studio-embed entry. |
| Brink backlog tracked in GitHub Project #6 | 2026-06-02 | DELIVERED | Project #6 (owner Syynth, "Brink") exists with Todo/In Progress/Done/Icebox; all seven area labels + severity + needs-design present. Partial gap: the pump encodes the needs-design skip but nothing encodes "skip Icebox". |
| Book structure: separate core toolchain from integrations/clients | 2026-06-03 | DELIVERED | Merged PR #34; `docs/book/src/SUMMARY.md:5` "# The Toolchain" and `:52` "# Integrations & Clients", with bevy/web/studio placed per the ruling's dividing line. |
| Book playground: embed the full brink-studio (standalone static build) | 2026-06-04 | DELIVERED | Merged PR #36; `vite.config.embed.ts` app-mode config; `justfile:127/131` builds and stages `dist-embed`; `book.yml` gained pnpm/node + `just book-assets`. |
| Publish the full brink Rust toolchain to crates.io (release-plz + cargo-dist) | 2026-06-04 | DELIVERED | Merged PR #42; `Cargo.toml:98-117` path deps with versions; four named exclusions carry `publish = false`. Deviation: the mandated "no semver guarantees" note lives only in `docs/releasing.md:34`, invisible to a crates.io reader. |
| Harden the release/CI supply chain (OIDC, SHA-pinned actions, cargo-deny) | 2026-06-04 | DELIVERED | Merged PR #42; `release-plz.yml:41-47` OIDC + `crates-io` environment; `ci.yml:9` `permissions: {}`, `:331-341` cargo-deny SHA-pinned; deny.toml, dependabot. Deltas: toolchain now 1.97.1 (was ruled 1.95.0); generated `release.yml` is the one non-conforming island. |
| State View panel is a read-only runtime debugger | 2026-06-04 | DELIVERED | Merged PRs #64, #70; `packages/studio-ui/src/StateView.tsx` has no setter; `brink-runtime/src/debug.rs:23` `DebugSnapshot`. Transcript moved to the session layer — narrowed by the next day's two-views ruling, not dropped silently. |
| Runtime changes deferred to separate design issues | 2026-06-04 | DELIVERED | The named instance (#62) was filed and closed as ruled; the practice is still live (`docs/design/yield-time-terminal-classifier.md:4`, open #1520, #1212, `BRINK-CONFIG.md:19`). Gap: never codified as a standing rule in CLAUDE.md or the contributing docs. |
| Studio gets two views: State (runtime) + Program Explorer (compiled) | 2026-06-05 | DELIVERED | Merged PRs #70, #71; both views exist and are wired; `brink-runtime/src/debug.rs:23` public API replaced the testing-feature hack. **Gap:** the State→Program-Explorer cross-link was not built and #69 scopes only to editor source. |
| Program Explorer reuses brink-format's write_inkt | 2026-06-05 | DELIVERED | `brink-web/src/story_runner.rs:102` and `session.rs:134` both call `brink_format::write_inkt` — no new disassembler. Refined by the Compiled Output document (merged PR #132); structured tables followed via `program_model.rs`. |
| External-binding work splits into a runtime/web track and a deferred tooling-manifest track | 2026-06-09 | DELIVERED | Merged PR #73; `brink-web/src/story_runner.rs` bind/unbind, get/set_var, set_seed, save/load bytes; `format/src/save.rs:32` `SAVE_FORMAT_VERSION`. Phase 2/3 landed additively as designed. |
| Track B host-capability manifest: tooling-only, two-source enrichment | 2026-06-09 | DELIVERED | Merged PR #74; `brink-ir/src/host_manifest.rs:36`; inline-wins-on-conflict at `external_check.rs:239` with a test. **Orphaned sub-commitment:** the insert-EXTERNAL code action does not exist and no issue owns it (may be dead by intent). |
| Studio shell redesign direction | 2026-06-10 | DELIVERED | Merged PRs #98, #99, #103. Spec merged 15:54, first implementation 16:08 — spec before code. `packages/studio-shell` has regions/toolwindow/strip-drag/command/palette/themes; no dockview dependency. |
| Studio shell spec: open questions resolved (Program Explorer, menus, keybindings) | 2026-06-10 | DELIVERED | Merged PRs #132, #112, #99. Program Explorer stays a tool window; the .inkt dump is a read-only editor document; hamburger menu generated from the command registry; keymap = registry defaults + user-override JSON. |
| Studio shell: notification service replaces Toast | 2026-06-10 | DELIVERED | Merged PR #118; `packages/studio-shell/src/notifications.ts` — severity timeouts, max-visible 3 + overflow, history limit 100, hover-pause, command-only actions; bell with unread badge. |
| Studio shell: embedder extension API (host-provided panels) | 2026-06-10 | DELIVERED | Merged PR #140; `studio-shell/src/extensions.ts:51`/`:88`; `command.ts:28/36/68` host-id namespacing enforced in **both** directions with rollback. Versioned `StudioPublicState` facade; hosts never touch the store. |
| Studio shell: Story Graph document (visual story explorer) | 2026-06-10 | DELIVERED | Merged PRs #139, #143; `brink-ide/src/story_graph.rs:41` END/DONE node ids + node/edge kinds, deterministic ordering, function knots excluded; TS side is a read-only editor document with session overlay from `debugState` only. |
| Studio shell: story session as a first-class concept; session-bound views | 2026-06-10 | DELIVERED | Merged PR #106; `studio-store/src/slices/session.ts`; lifecycle commands with `when` predicates; recompile choice-replay with truncate-on-divergence. No player runner field remains. |
| Studio shell: Location/navigation protocol; Settings document; shared overlay | 2026-06-10 | DELIVERED | Merged PRs #104, #136, #100. Four-space Location union + resolvers + `editor.reveal`; SettingsDocument; floating-ui Overlay used by all four ruled surfaces. Residual: only the `symbol` resolver is registered — `program`/`session` deferred to their consumers, tracked at open #69. |
| Studio shell implementation scoped as 20 issues across 6 milestones | 2026-06-10 | DELIVERED | Issues #78–#97 all CLOSED with Shell Phase 1–6 milestones; #76 closed as superseded by #84 exactly as ruled. **Caveat:** the #69 re-framing was never written into the issue, and the behaviour is still unbuilt. |
| Doc-comment blocks are part of their declaration for editor-structural purposes | 2026-06-10 | DELIVERED | Merged PR #101; `brink-ide/src/folding.rs:315` `push_decl_fold` (one whole-line fold from the doc-block start, no double-fold); collapsed line renders the hidden header; structural moves/deletes and knot-view slices all start at `doc_extended_start`. Regression tests present. |
| Player-as-document confirmed (was tentative) | 2026-06-10 | DELIVERED | Merged PR #133; `packages/brink-studio/src/mount.tsx:437` registers `PLAYER_TYPE_ID` as a document type; opened in a right split at bootstrap. Sequencing held (#122, #90 closed first). Spec §4 updated. |
| Multi-document support fixed at the wasm session layer (document handles) | 2026-06-10 | DELIVERED | Merged PR #124; `brink-web/src/editor/doc_handles.rs:10/23/36/54/103`; `update_document` returns a change-spec JSON consumed per-(doc,group). Caveat: the singleton API was not deleted and now has no production TS caller — dead wasm surface. |
| Editor split duplicates the active editor (VS Code exact) | 2026-06-10 | DELIVERED | Merged PR #128; `studio-shell/src/editor-commands.ts:32-35` (`Mod-\`), `editor-groups.ts:344` `splitGroup`, `ink-editor/src/document-sessions.ts:1005` `mirrorEdit` with `syncAnnotation` echo-suppression; open/reveal searches all groups. |
| Session-bound views stay provider-agnostic (live-inspector direction) | 2026-06-10 | DELIVERED | `studio-store/src/session/types.ts:146` `SessionProvider` + capabilities + `sessionDegraded()`; `story-commands.ts` gates every `story.*` on `can(cap)`; `StoryRunner` no longer appears in `studio-ui/src`. Degraded mode is first-class. Design issue #127 closed with the spec. |
| Editor-group maximize replaces player fullscreen | 2026-06-10 | DELIVERED | Merged PR #133; `studio-shell/src/maximize-commands.ts:25`, `editor-groups.ts:410`, Escape restore, mutual exclusivity with tool-window maximize. Player fullscreen retired; two stale doc/CSS comments remain. |
| Story Graph renders with react-flow | 2026-06-10 | DELIVERED | Merged PR #143; `packages/studio-ui/package.json:21` `@xyflow/react`; `StoryGraphDocument.tsx:379` renders `<ReactFlow>`; `story-graph-layout.ts:1-16` documents dagre as "off the render path", memoized on structure only. |
| Host functions panel inserts calls, not declarations; surfaces manifest metadata | 2026-06-11 | DELIVERED | Merged PR #147; `example-extension.tsx:254`/`:465` build `~ fn(args)`; the panel test asserts no "EXTERNAL" in the payload. Spec §8.2 amended. |
| Embedder file egress: mount callback + facade pull, dirty summary in state | 2026-06-11 | DELIVERED | Merged PR #159 (closed **both** #154 and #137, as the ruling required); `mount.tsx:150/168`, `StudioApi.tsx:168`, `file-change-hub.ts` as the single notify seam, `documents.ts:26-27` dirty scalar, Mod-S binding with tests. |
| Milestone structure for host-integration / live-inspector work | 2026-06-14 | DELIVERED | Milestones 7/8/9 exist with the ruled titles and correct issue layering; all Phase 7/8/9 issues closed. Shared channel is real code (`session/types.ts:146`), spec added by merged PR #183. |
| Phase 7 execution autonomy — merge-on-green for impl, review gate for design | 2026-06-14 | DELIVERED | Structural half confirmed in the PR record (one PR per issue, per-issue branches, sequential merges). **The review-gate half could not be verified** — GitHub shows reviews=0 on both the design and implementation PRs. Authority was time-boxed to a now-complete phase. |
| Studio QoL milestone — grouping | 2026-06-14 | DELIVERED | Milestone #10 exists; membership matches the ruled list exactly, including the #164 move; the ruled exclusions (#72, #119, #152, #163, #166) all report milestone=none. |
| Public diagnostic contract — resolve FileId→path at the compile() boundary | 2026-06-14 | DELIVERED | Merged PR #190; `brink-compiler/src/lib.rs:32` `ResolvedDiagnostic` with the exact ruled shape (+ an additive `severity`); resolution once at `driver.rs:25-40`; no pre-baked line/col; whole public family honours it. |
| Web/studio replay adapter — in-place StoryRunner reload | 2026-06-14 | DELIVERED | Merged PR #194; `brink-web/src/story_runner.rs:556` `reload` keeps bindings/seed/recorder and errors without disturbing the running program; `local-provider.ts:302` calls it on startSession. Evolved to `StorySession::reload` per the #370 entry. |
| Knot parameter arity in the compiled format (#178) | 2026-06-14 | DELIVERED | Merged PR #195; `format/definition.rs:33` `param_count: u8`, populated by codegen; both ruled arity checks live (`flow_instance.rs:673-680`, `story/mod.rs:448-455`); format-version bump recorded in the VERSION history. |
| Recompile keeps auto-reload; #181 degraded mode is mechanism-only | 2026-06-14 | DELIVERED | Merged PR #198; `brink-web/src/compile.rs:166` `program_checksum`; `session/types.ts:69` `sessionDegraded`; StatusBar + graph-overlay degraded states. Nothing beyond mechanism built, as ruled. |
| #182 spun into its own spec, sequenced last | 2026-06-14 | DELIVERED | `docs/multi-session-spec.md:1-8` exists and cites this decision. Caveat: the spec landed in the *same* commit as the implementation, so the design pass was concurrent rather than gated; #200 later closed after #182. |
| #182 multi-session: registry of single-session providers | 2026-06-14 | DELIVERED | Merged PR #199; `slices/session.ts:79-81` `sessions`/`activeSessionId`; `StatusBar.tsx:166` hides the picker at ≤1 session; single-session `SessionProvider` unchanged. |
| Pull shared-context flows forward as a local studio feature (#200) | 2026-06-14 | DELIVERED | Merged PR #201; `story/mod.rs:63` `shared_instances`, `:907` `spawn_flow_shared`, `:944` `continue_flow_single`; isolated `spawn_flow` retained for bevy-brink; picker keeps both kinds. Regression tests both sides. |
| Phase 9 host-aware argument picker — Tier-3 design (static-first) | 2026-06-15 | DELIVERED | Merged PRs #202–#206; `host_manifest.rs:112` advisory `values` separate from `constraint`, never read to harden a diagnostic; push-cache `set_host_values`; data-only `argumentProviders`. Static merged before dynamic, as ruled. Minor shape narrowing on `enumerate`. |
| Argument-widget authoring — Fill and Form are distinct entry points | 2026-06-15 | DELIVERED | Merged PR #220; `argument-widgets.ts:276` EditWidget, `:342` FillGhostWidget, `:827` FormGlyphWidget + `:99` openCallForm — three separate mechanisms, never merged. Residual gap: the host whole-call widget override is specced but unimplemented, no issue. |
| Argument-widget forks resolved — inline is data-only, panel launches the Form | 2026-06-15 | DELIVERED | `wasm-types/src/index.ts:520/528-536`; studio owns popover/focus/close and calls `widget.editor.render(...)`; auto-open is a live opt-in defaulting to false. Fork (4) resolved by the later Form-launchers ruling, which is what the code implements. |
| Value picker is a label-searchable typeahead with a two-path focus rule | 2026-06-15 | DELIVERED | Merged PR #269; typing path at `completions.ts:114-119`, click path at `argument-widgets.ts:586/643`. Two residual sub-clauses unbuilt and unowned: the list is not virtualized, and an **empty** value-list slot gets no Fill affordance. |
| Form launchers: hover-card action is the default; glyph + auto-open opt-in | 2026-06-15 | DELIVERED | Merged PR #220; all five launchers present with ruled defaults (`FormGlyphMode` default `"off"`, `Mod-Shift-a`, unconditional hover action); live switching via StateField/StateEffect; storage key as specified. |
| Argument Form is a typed per-argument widget collection with live inter-arg context | 2026-06-16 | DELIVERED | Merged PR #223; `argument-form.ts:220-244` documented precedence color → hostWidget → values → text; inter-arg context resolved from the live draft array, not the document. |
| The call Form is driven by signature metadata, not the live call-site | 2026-06-16 | DELIVERED | Merged PR #233 (closes #229); `brink-ide/src/argument_widgets.rs:29-33` `declared_groups` distinct from arg-state `groups`; one slot per declared parameter; Apply replaces the whole paren range, filling too-few and truncating surplus. |
| The studio file lifecycle (rename/move/delete) lives in brink-ide | 2026-06-16 | DELIVERED | Merged PR #252; `brink-ide/src/file_rename.rs:45` computes inbound + outbound INCLUDE rewrites; studio is thin. Three deltas: result type unified as `StructuralResult` (#316); file rename does not use the binder seam; **whole-file delete/create never moved into brink-ide and have no dangling-INCLUDE gate — unowned**. |
| Normalize `..` in include-path resolution system-wide | 2026-06-16 | DELIVERED | Merged PR #252; `brink-db/src/db.rs:778` + `normalize_path` at `:790`; bevy-brink duplicate at `source_loader.rs:304` citing the ruling; brink-driver test rewritten to the new behaviour. |
| Structural-move same-file ref edits folded into new_source via slice-local application | 2026-06-19 | DELIVERED | Merged PR #302; `brink-ide/src/structural_move.rs:461` `apply_window`; all three ops route every verbatim slice through it. The `// TODO: adjust same-file ref offsets` string is gone. Regression tests at three call sites + CLI level. |
| Studio symbol Rename is safe-by-default with an in-place breakage report | 2026-06-20 | DELIVERED | Merged PR #306; `brink-web/src/editor/refactor.rs:471` `rename_safe`; `SymbolRenamePrompt.tsx:137-172` alertdialog breakage report with clickable `path:line:col` and exactly two actions. No upfront unsafe checkbox, as ruled. |
| F2 inline rename is a full cross-file, safe-by-default rename | 2026-06-20 | DELIVERED | `refactor.rs:483-497` `rename_symbol_at` routes through the same `rename_safe`; the old single-file path was dropped. **Caveat:** the presentation is an inline CM widget, not the ruled store-driven prompt (#323/#324) — a deliberate later relocation that no log entry records. |
| Editor round 2 (celeris feedback): design-first, single 0.8.0 release | 2026-07-05 | DELIVERED | Shipped as one 0.8.0; design PR #384 merged before build PRs #386/#385. Class taxonomy is the open string-keyed scheme (`docs/editor-consumer-guide.md:473`). #362 correctly stayed parked and is still OPEN. |
| Story Session (#370): the journal is Rust-canonical; web and bevy are bindings | 2026-07-05 | DELIVERED | Merged PRs #385/#389/#393/#401; `brink-runtime/src/session.rs` — observation-only journal, v1 embedded checkpoint, reserved flow-tag dimensions, typed snapshot/diff, turn-boundary set_var/go_to_path/load_state, documented bypass escape hatch. No JS-side journal. |
| Dialogue dialect (#368): authoring/tooling artifact only — never runtime-delivered | 2026-07-05 | DELIVERED | Merged PR #386; three-valued `ElementNature`; portable-regex core with affix sugar; opt-in reference `DialectParser`; shared conformance corpus driven from both Rust and TS. No dialect payload in brink-format. |
| #411 scratch-eval externals policy: harness-built handler; presentation = effect | 2026-07-06 | DELIVERED | Merged PR #455; `brink-runtime/src/external_policy.rs:88` `KindTieredHandler`, manifest-blind; presentation-can-never-be-Query enforced by wire-type shape; async pending + AbortSignal shipped. **Orphaned sub-commitment:** the concurrency cap (spec proposes 8) was never built and no issue owns it. |
| Scoped flow state: promote per-flow/world state model into the runtime core | 2026-07-06 | DELIVERED | Merged PRs #446/#448/#450/#451; `world.rs:136/254/520/768`, `commit` seam at `:865` returning `NotImplemented` as ruled. Two sub-commitments amended in the spec rather than silently dropped: no `FlowStep` type; the isolated/shared map collapse is partial and deferred with no owning issue. |
| HIR span overlay in editor + source-compatible debugger direction | 2026-07-07 | DELIVERED | Merged PRs #465/#467/#469; `brink-ide/src/hir_projection.rs` + `packages/ink-editor/src/hir-overlay.ts:148-155` data-attributes. Deferred half correctly tracked at open epic #452. |
| Shared read-only HIR visitor + walker unification | 2026-07-07 | DELIVERED | Merged PR #464; `brink-ir/src/hir/visit.rs:74` `HirVisitor` with opt-in Expr descent; four named migrations verified in code; ordering honoured (#464 merged before #465). Known coverage gap documented in-source: tag contents are never descended. |
| Layered HIR structural architecture: projection as the canonical model | 2026-07-07 | DELIVERED | Merged PRs #465/#471; `brink-ide/src/hir_projection.rs:1` "the canonical structural model"; per-line weave view first-class; folding + line_context are views, not walkers. Two self-documented coverage lags (block folds bypass spans; inline branches emit one container per construct). |
| F5 speculative-eval: mechanism-agnostic API on the simplest correct impl (B) | 2026-07-07 | DELIVERED | Merged PR #461; `brink-web/src/compile.rs:213` `compile_fragment`. `OverlayProgram`/`ProgramLike` left no dead code. Deviations: symbol is `__eval_<hash>` not `$eval_<h>`; cache key is 2-tuple not 3-tuple. #411 still OPEN despite #440's closing comment. |
| Book code examples must be compile-checked | 2026-07-10 | DELIVERED | Merged PR #477; `ci.yml:547-551` runs `mdbook test`; `justfile:67` `book-test`. Counted at this ref: 42 plain rust fences, 4 no_run, 25 remaining `ignore` — of which **17 carry no justification**, which is what the ruling asked to eliminate. No issue owns the remainder. |
| bevy-brink re-exports all public-signature brink_runtime types | 2026-07-10 | DELIVERED | Merged PR #470; `bevy-brink/src/lib.rs:89` `pub use brink_runtime as runtime`, `:98` `World as BrinkWorld`, `:118` the re-export list. No public signature names an un-re-exported path. Caveat: no automated guard against drift. |
| Bevy book examples become compile-checked doctests, backed by `book-test` | 2026-07-10 | DELIVERED | Merged PR #470; `justfile:67`; bevy pages use `{{#include}}` from `crates/bevy-brink/examples/` checked by `cargo build --example` — a variant mechanism, documented. Residue: three bare `rust,ignore` fences in `handles.md` checked by nothing. |
| Flow-private state: runtime capability first, language feature later | 2026-07-10 | DELIVERED | Merged PR #495; `brink-runtime/src/world.rs:254` resolves `base ⊕ host-overrides` literally, seeding from `program.local_scope_defaults()` before host overrides. Compiled side no longer empty (#473 closed). Stale doc at `bevy-brink/src/globals.rs:227`. |
| Entity flows persist state-only via per-flow SaveState | 2026-07-10 | DELIVERED | Merged PR #490; `brink-runtime/src/save.rs:74`/`:187` free functions over any context, with `Story::*` as thin wrappers; bevy composes per-flow via `ContextView`. Format constant still v1. The "resumes from knot entry" clause was not independently verified — UNKNOWN in detail. |
| Scoped-state policy model (final): per-World, default World, subtree-inclusive knot scope | 2026-07-10 | DELIVERED | `brink-runtime/src/world.rs:136` `WorldPolicy`, `:254` `resolve`, `:440` `expand_knot_scope`, `:520` `World::new`. `Scope` defaults to World; spawn takes no policy. Assumed home also delivered ahead of expectation via #473/PR #495. |
| F6 slicing — value now, language later, no rework | 2026-07-10 | DELIVERED | Merged PRs #475/#481/#484/#488/#490; all five slices verified in code (shared drive op with reconciled limits, save/load lifted off Story, subtree policy, `BrinkContext = { inner: FlowLocal }`, per-entity durability). `commit_*` helpers deleted. |
| #479 machinery/narrative folding: container-bounded runs, opt-in + gated | 2026-07-10 | DELIVERED | Merged PR #487; Choice/Gather-only bounding at `brink-ide/src/folding.rs:668` with the deliberate branch-container exclusion; `DEFAULT_ACTIVE_KINDS = {structural}`; `fold_runs_enabled` false by default. Host wiring is out of repo, as framed. |
| LineContext.has_tags is true for tagged choice lines | 2026-07-10 | DELIVERED | `brink-ide/src/line_context.rs:570` sets `has_tags = true` unconditionally on every Tag span, citing this decision; regression test present. Shipped under a different issue (#480), which is why an issue-grep audit would miss it. |
| Annotations ride the tag channel; declarations marked via above-line directive tags | 2026-07-10 | DELIVERED | Merged PR #495; `brink-ir/src/hir/lower/content/tag_line.rs:26` erases directives from runtime tags with E045/E046/E047; relaxed above-line attachment covers all four ruled declaration kinds. Channel has grown well past #473. |
| Scripting-epic direction: #473 first, then query-substrate phase 0 | 2026-07-10 | DELIVERED | Merged PRs #495/#509/#515 in the ruled order (#495 merged 40 min before slice A). #500, #501 closed; `signature(def)` is a real per-def query. Ratchet is 5607, up from the 5,577 the ruling anchored on. **One sub-clause UNKNOWN:** "LIR logic/narrative split hygiene" — no commit or PR performing it was found. |
| Phase-0 query engine: salsa, adopted coarse-grained | 2026-07-11 | DELIVERED | Merged PR #515; `brink-db/src/queries/mod.rs:292` `#[salsa::input]`; every named query exists. `Arc<plain>`-not-tracked-structs clause still holds and was re-ratified. The "coarse granularity" clause is superseded by the fine-grained commitment. |
| Fine-grained salsa + IR rework is the eventual destination | 2026-07-11 | DELIVERED | Merged PR #637; `brink-db/src/db.rs:629` `inferred_signature(def)`; per-def/per-SCC ingredients present. Promoted past tentative by the 2026-07-13 entry; epic #623 CLOSED. The "per-def hashes in hir" sub-clause was superseded by Eq-based backdating, not orphaned. |
| Error handling v1: ink-side infallible; errors are host events | 2026-07-11 | DELIVERED | `brink-runtime/src/error.rs:145-149`, `:279-285`; no in-language error values; host surfacing disjoint in `bevy-brink/src/batch.rs`. **Gap:** the "recorded in the journal" clause was never implemented (`session.rs` `EventKind` has no fault event); goal still holds by determinism. No issue owns it. |
| Value-model round: closing ratifications | 2026-07-11 | DELIVERED | `format/src/value.rs:1026` `MapKey` (int/str/bool) + `OrderedMap`; `ProjSegment::Index`; `Value::Handle` (merged PRs #763/#780); `EntityMapper` hook in bevy; `ptr_eq` fast path. Round took exactly one format bump (VERSION 4). |
| Wasm-observable behavior changes require a @brink-lang/web changeset | 2026-07-11 | DELIVERED | `CLAUDE.md:162` and `.claude/skills/autonomous-pump/BRINK-CONFIG.md:14` both carry the rule verbatim. **Gap:** no mechanical enforcement — no CI check, no issue proposing one; a crates-only PR that skips the changeset fails silently. |
| Structs: VERSION 4 reserves the surface; implementation rides the typed dialect | 2026-07-11 | DELIVERED | Merged PR #552 (reservation), #664 (implementation). `format/inkb/mod.rs:103` `VAL_RECORD`, `:245` `StructShapes`, record opcodes 0xCE–0xD2 — materialized **without** a bump, so the one-bump rule demonstrably paid off. Tail tracked at open #620/#665. |
| T1b surface rulings: strict-ink default, pure-logic blocks, sigil literals, lowercase stdlib | 2026-07-11 | DELIVERED | Merged PRs #572/#575/#579; `brink-project-config/src/lib.rs:119` `Dialect` defaults StrictInk; `brink-ir/src/hir/types.rs:507-535` documents `BlockStmt` as a **closed** set — the fence is by construction; E055 lvalue rule. |
| Stdlib totality rulings: contains() false on non-key needles; mutator arity a compile error | 2026-07-12 | DELIVERED | Merged PR #1586 (closed #582) shipped the deferred E152 warning at `brink-analyzer/src/contains_domain.rs:1`. **Caveat:** strict-mode only — under `types = gradual` it stays silent, narrower than the ruling's unqualified wording. E058 present from PR #584. |
| brink-fmt block formatting: blank-line-run collapsing ratified | 2026-07-12 | DELIVERED | Merged PR #604; `brink-fmt/src/render.rs:636` documents and implements collapse-to-one at all three guard sites, with tests. Exact-count preservation stays parked at open #592 (icebox), as ruled. |
| Language-facility doctrine: standard functions vs intrinsics vs syntax | 2026-07-12 | DELIVERED | Merged PR #606; applied and cited by name in `docs/t1b-surface-spec.md:154`, `t1c-spec.md:84`, `flow-suspension-spec.md:49`, and analyzer call sites. **Caveats:** the doctrine text lives only in the decision log — no spec section, no CLAUDE.md rule, no CI check; and it was already violated once in practice (#880). |
| T1c rulings: function values are partial application, not closures | 2026-07-12 | DELIVERED | Merged PRs #697/#707/#720/#730/#740/#749; tracker #645 CLOSED. 8 of 9 items shipped. **Item (8) is contradicted by the shipped design** — see CONTRADICTED row 12; `docs/t1c-spec.md:166` is stale spec a future agent will implement against. Item (6)'s destination #597 closed as superseded into open #1210. |
| Typed-mode rulings (#605 round) | 2026-07-12 | DELIVERED | Merged PRs #607/#640/#720/#730; all four sub-rulings hold; sequencing held in wall-clock order. Struct decl body is the amended `#{…}` form. **Bookkeeping gap:** TM-4 trackers #620/#665 remain OPEN though the work landed — stale, will mislead triage. |
| Struct declaration syntax amended: decl body mirrors the construction literal | 2026-07-13 | DELIVERED | Merged PRs #622, #725; `brink-syntax/src/parser/declaration.rs:308` documents the grammar as `STRUCT Name = #{ field: type, … }` and `:322` `struct_declaration` parses it, with trailing comma + multi-line; brink-fmt half landed (#669 closed). Governs the **ink** dialect only — see the SUPERSEDED row for the native reading. |
| Salsa fine-grained migration promoted: tentative → committed | 2026-07-13 | DELIVERED | Merged PRs #634/#637/#655/#661/#753/#798/#812/#823/#837/#835; `brink-db/src/queries/mod.rs:1571-1590` FG-4d per-container chunk memos + link; epic #623 CLOSED before #397. Tracked structs declined (superseded in part). **#633 still OPEN** despite all children merging. *(Traced twice; assumed-home check reached the same verdict.)* |
| Fine-grained salsa rulings (#623 round) | 2026-07-13 | DELIVERED | Merged PRs #634/#637/#661/#753/#798/#812/#823/#837/#846; SCC batching as derived queries (no salsa `cycle_fn`), `Arc<plain>` keyed by interned `DefKey`, `inferred_signature(def)` with real LSP consumers; ordering honoured. **#633 stale-open.** |
| TM-2 rulings: E063 opt-in until TM-3; strict-ink suppresses annotation content checks | 2026-07-13 | DELIVERED | Merged PR #656; `brink-analyzer/src/strict.rs:302`/`:183` promote E063 under Strict only; `lib.rs:630` gates content checks on `Dialect::Brink`, quoting the ruling. E062 half is moot — retired by T1c-1. |
| TM-3 design note resolved: `Ty::Conflicted` lattice point added to `unify()` | 2026-07-13 | DELIVERED | Merged PR #656; `strict.rs:789` emits E066 for `Escape::Conflicted` from `strict::check`, consuming the #627 lattice rather than re-deriving. Advisory consumers still treat Conflicted like Unknown, as ruled. |
| FG-2.1 rulings (#638 round): pre-scan read-set, full narrowing, before TM-3, one PR | 2026-07-13 | DELIVERED | Merged PR #655 (single PR); `infer/mod.rs:845` `referenced_globals`, `:805` `call_edges`; T2 effects reuse actually happened; sequencing honoured (#626 → #655 → #656 same day). **Caveat:** the third gate was weaker than the wording implies, by the repo's own later admission. |
| TM-3 completion rulings (#659 round): int()/float()/string() conversion intrinsics | 2026-07-13 | DELIVERED | Merged PR #694; `format/src/opcode.rs:220-222` CONVERT_INT/FLOAT/STRING; permissive domains + turn-terminating faults; truncate-toward-zero citing the ruling; E078 under strict; author-shadowable. One stale doc nit (opcode.rs:2598 says 0xE2). |
| T1c spec rulings (draft round) — items (1)(2)(3) | 2026-07-13 | DELIVERED | `infer/body.rs:1276` `check_value_call`; `value_ops.rs:170-171` display form pinned by a hover test; `error.rs:259-262` `FunctionValueRehydrationMismatch` for save rehydration. *(Item (4) is CONTRADICTED — separate row.)* |
| Parallel agent dispatch runs through the autonomous-pump process | 2026-07-13 | DELIVERED | Merged PR #547 vendored the skill; `.claude/skills/autonomous-pump/{SKILL.md,BRINK-CONFIG.md,pump.js}` present and still evolving on main. Ruled elements present under evolved names; "manifest"/"watcher" wording has no literal counterpart (renamed, not missing). |
| Struct construction literals: source-order evaluation, duplicate field a compile error | 2026-07-14 | DELIVERED | Merged PR #741; duplicate-field shipped as **E084** (not the E083 the PR body claimed), fires in both type modes; source-order staging at `codegen-inkb/src/expr.rs:488` pinned by tests. #675/#676 closed. |
| Modules & visibility rulings (#719 round) | 2026-07-14 | DELIVERED | Merged PRs #764/#770/#778/#781/#782/#799; epic #719 CLOSED. Declaration-flips-the-default, IMPORT forms, dialect gating, stem-collision E085, `#@was` alias table on the wire, dev-tooling override — all verified. **Two sub-commitments now tracked at open #1671/#1672; the intl alias rider is ORPHANED (see above).** |
| Handle rehydration mechanics + handle-parameterized capability atoms | 2026-07-14 | DELIVERED | Merged PR #780; `bevy-brink/src/handle.rs:67` `HandleKind` with `save_key`/`resolve`; registries + persistence; never-fail load with `RehydrationPolicy`; quiescent GC; **reserved wire slot enforced, not merely documented** (`inkb/read.rs:893-895` rejects non-None). |
| Drop the converter pipeline before VERSION 4 | 2026-07-11 | DELIVERED | Merged PR #548 (#544 closed). Verified at this ref: `crates/internal/` has 18 crates, none of them converter/json; CLI emits ".ink.json ingestion was retired". **Doc residue:** `brink-ir/src/lir/mod.rs:8-10` still says "Two backends consume the LIR" including a JSON backend that no longer exists. |
| Cross-module name collisions: hard-error stopgap now, import-scoped resolution committed | 2026-07-14 | DELIVERED | Merged PR #793 (E096 stopgap, 21:23) then **PR #811 (endgame, 23:08 the same day)** — this did *not* become the classic stopgap-orphan. `brink-analyzer/src/manifest.rs:196` documents the relaxation with tests; import-scoped resolution in `resolve.rs:40-127`. |
| FG-5 memory bounding: runaway ceilings only — steady-state LRU rejected by data | 2026-07-15 | DELIVERED | Merged PR #835; per-file `lru = 4096` on 8 families, per-def `lru = 16384` on the DefKey families, all doc-commented to #647; five named heap_size estimators wired. **Stale doc defect:** `brink-db/src/memory.rs:20-25` still claims no query specifies an estimator. |
| T1e ratified as current posture; narrowings recorded as exploration intents | 2026-07-15 | DELIVERED | Merged PR #818; `docs/t1e-spec.md:1-11` carries the ratification header. Both narrowings enforced in code (E097 at `ref_projection.rs:143` with a regression test; root-cell effect rows at `infer/body.rs:757-765`). All three iceboxes open as recorded. |
| Single compile pipeline: the legacy one-shot path is removed | 2026-07-15 | DELIVERED | Merged PR #844; `brink-compiler/src/driver.rs:115` pulls the memoized `story_data`. Two sub-clauses deliberately not executed and **already recorded as such** in the same-day cleanup-tail entry (deletions declined after audit) — superseded-with-rationale, not orphaned. |
| T2 effects rulings (sitting 2): no lockfile, assertions-only, default-public entries | 2026-07-14 | DELIVERED | Merged PRs #877/#894/#890; `effects_assertions.rs` (E103 exceedance-only, no lockfile artifact); `brink ide effects-diff` with CI flags; IDE hover; `queries/mod.rs:2343` `is_entry: !is_private(def)` asserted both ways. One sub-clause unenforced: no runtime consumer refuses a `#@private` host lookup. |
| T2 host mechanics settled: borrow-not-copy, frame-start consistency, two-level bevy | 2026-07-15 | DELIVERED | Merged PRs #906/#920/#934/#1642; compiler half merged before host half, as ruled; rows-as-Access currency, UnsafeWorldCell-scoped stepping, deterministic flow-id write ordering. Two named pieces (prefetch, SystemParamBuilder v2) unbuilt but **owned by open #1062**. |
| T2 sitting 4 (final): host-driven sleep with the wake contract, opaque-strings manifest | 2026-07-16 | DELIVERED | Merged PRs #991/#906; `bevy-brink/src/sleep.rs:261` `FlowSleep<M>` with Persistent/Once/Latch, re-evaluate-don't-wake, dormant spawn; `capability.rs:100` opaque `detect` map. **Orphaned sub-item:** the ruled serial-mode book section was never written and epic #897 is closed. |
| Bevy host track: sanctioned-unsafe exemption, pump-driven with gates, harness baseline-first | 2026-07-16 | DELIVERED | Merged PRs #907 (baselines, 07:06) then #934 (10:15) — baseline-first held. One `expect(unsafe_code)` in one module with per-block SAFETY arguments; the determinism law as a randomized-workload gate. The human "drive-it" gate leaves no artifact — neither confirmed nor contradicted. |
| Map/record equality is insertion-order-insensitive | 2026-07-18 | DELIVERED | Merged PR #969; `format/src/value.rs:1199` hand-written `impl PartialEq for OrderedMap` (len + per-key lookup); derived Eq removed; membership follows by content. Iteration order left insertion-ordered as ruled. Hardened by PR #1038. |
| Pump communication is durable-by-default: findings go to GitHub | 2026-07-18 | DELIVERED | `BRINK-CONFIG.md:39-43` and `pump.js:154/165/185/219-220` encode all four roles, the ledger issue and the three labels; labels exist; #967 has wave-ledger comments; live practice visible on recent PRs. **Two caveats:** the ledger rung lapsed after w32, and `pump.js:38` ships `LEDGER = null` so the ledger comment silently no-ops unless the operator fills it. |
| Multi-marker capability scoping: global manifest + per-marker registries, validated at load | 2026-07-18 | DELIVERED | Merged PRs #989, #1014; `bevy-brink/src/capability.rs:654` `check_load_capability_gate`; every story-construction path gates before building a FlowInstance. #1014 closed a real hole (hot-reload bypass) and collapsed both sites onto one helper, with the history recorded in-source. |
| Detect-bit merge across calls in one container: AND (conservative) | 2026-07-18 | DELIVERED | Merged PR #991; `bevy-brink/src/capability.rs:509-521` folds with logical AND citing #913 and this ruling; consuming field documented as AND-merged; #913 CLOSED. |
| Compound gameplay v2: BSP room-recipes, MGS-lenient suspicion, LOS escape, clock-as-consequence | 2026-07-18 | DELIVERED | Merged PRs #1033, #1049; every ruled mechanic in `demos/compound/src/`. **One gap:** the opt-in timed Vault is still spatial-only; its follow-up #1034 was hand-closed as COMPLETED with no PR and no timer code — effectively an undocumented "not wanted". |
| Compile error-gate is entry-closure-scoped; whole-project has_errors stays for IDE | 2026-07-18 | DELIVERED | Merged PR #1048; `brink-db/src/queries/analysis.rs:873` `has_errors_in_closure_query`; artifact path gates on it; whole-project query untouched for IDE surfaces. Behaviour pinned by a test quoting the ruling. #1052 resolved by a different route (Project::load), consistent not contradictory. |
| Option[T] pulled forward as a compiler-known builtin; the absence doctrine | 2026-07-18 | DELIVERED | Merged PR #1118 (closed #1107); `Ty::Option`; every ruled flip returns Option in checker and VM; `x or default` short-circuits (PR #1479); display-boundary forgiveness (PR #1465). Spelling partly superseded by the angle-bracket entry; open #1552 owns that sweep. |
| The protocol registry: closed, compiler-declared, three v1 entries with effect contracts | 2026-07-18 | DELIVERED | Merged PR #1124; `brink-analyzer/src/protocols.rs:58` `Protocol` with `ALL: [Protocol; 3]`, receiver/contract data, E113 name reservation wired into the pipeline. **Caveats:** `check_protocol_impls` has no non-test caller (impl spelling is the deferred piece — see ORPHANED 9); the later "construction is the 4th entry" ruling is not reflected in `ALL`. |
| Mutation posture: UFCS auto-ref on lvalue receivers + imperative/past-participle naming | 2026-07-18 | DELIVERED | Merged PR #1524 (closed #1462); `brink-analyzer/src/ufcs.rs:238` `FreeFnAutoRef` + `:719-727` gate + E143. Prelude mutators reach mutation via RMW (PR #1514). **Two unowned gaps:** the ruled rvalue-receiver compile error is not implemented (the grammar cannot yet express it), and `reverse`/`reversed` is absent from the prelude. |
| The ordering doctrine: NaN faults in dev, pinned placement in prod | 2026-07-18 | DELIVERED | Merged PR #1149; `brink-runtime/src/story/call_stack.rs:255` `ExecMode{Dev,Prod}` with Dev default and the fence in its doc; `UnorderedComparand` gated on Dev across sort/min/max/heap_push with verb-named tests; E119 comparator gate. **Orphaned leg:** the ruled brink.toml knob does not exist — only the host API — and no issue owns it. |
| Random ruled: rng-as-cell, and rand::int made total by the first value refinement | 2026-07-18 | DELIVERED | Merged PRs #1125, #1136; RNG_CELL writes recorded as ordinary effects (no new dimension); `NonEmptyRange` type + Option-returning validator + E117. Two spelling deltas authorized by later entries (`non_empty`; flat `int(x)` pending `std::rand` namespacing — that leg is unbuilt with no issue). |
| Collections+ ruled: Weighted evidence-by-construction, the humble heap | 2026-07-18 | DELIVERED | Merged PR #1156 (closes #1113); `collection_ops.rs:797` weighted_new + heap verbs over arrays, no new value kind; owed E120 exists with the E078-style static/runtime split. Soft gap: `docs/diagnostics/E120.md` still has template placeholders. |
| Closers ruled: anonymous records retired, for-k-v iteration, assembled prelude, std:: tree | 2026-07-18 | DELIVERED | Merged PRs #1466, #1470, #1298; all seven items land in spec/code. **Stale finding to correct:** `docs/hir-admission-contract-findings.md:67` still claims the annotation grammar has not caught up, which `hir/lower/directive.rs:786` contradicts. **Gap to watch:** std::-vs-prelude namespace enforcement does not exist in code and no issue names it. |
| Findings batch 1 ruled: all seven recommendations adopted | 2026-07-19 | DELIVERED | Merged PRs #1149/#1124/#1125/#1136/#1466; each of F0/F1/F3/F6/F7/F8/F10 verified in code, not just changesets (imperative/functional twins, Display as the single dispatch seam, single-draw chance, hard E113, `Value::Range`, inert-in-gradual refinement, snapshotted key set). |
| F27/F28 ruled: Option has no truthiness; display renders totally until B4 | 2026-07-19 | DELIVERED | Merged PR #1133; strict = E116 compile error, gradual = runtime `OptionTruthiness` fault at the VM seam; A1's falsy-none removed with the supersession recorded in-code. B4 successor has since landed (`stringify_display`), closing the window as ruled. |
| Tower mini-spec ruled (T1–T5); A2 judgment calls confirmed; compare coherence | 2026-07-19 | DELIVERED | Merged PRs #1138/#1122/#1133; `docs/tower-mini-spec.md` exists; unaligned glam Vec2..Mat4 value kinds with LE-f32-lane wire, glam pinned once with serde off, tower not orderable; `@[effects(…)]` paren form live. **Orphaned residual:** the ruled brink.toml profile knob does not exist and no issue names it. |
| Typing posture ruled: native surface strict-only; strict becomes the brink default | 2026-07-19 | DELIVERED | `brink-analyzer/src/strict.rs:140` `resolve_type_policy` documented to this ruling (Brink→Strict, StrictInk→Gradual); native strict-only enforced as E137 with an end-to-end test; #1127 and #1342 closed. Residual native wiring tracked at open #1180/#1358. |
| `.brink` is the native-surface extension; editor + book are first-class NS workstreams | 2026-07-19 | DELIVERED | `brink-db/src/queries/mod.rs:410` extension check; merged PR #1187 native lexer/CST; module identity, discovery, LSP and config all accept `.brink`. Parts (2) and (3) correctly TRACKED at open #1131/#1350 and #1132 + chapter issues. |
| Lambdas ruled: Rust pipes under the RustScript north star; charter §7 amended | 2026-07-19 | DELIVERED | `brink-syntax-native/src/parser/expr.rs:243-306` — `\|x, y\| expr` / `\|g: Guest\|: bool { … }`, no `move`, `ref` deliberately never accepted (capture-by-value); charter §7 header carries the amendment. **Loud gap:** the "assignment to a captured binding is a compile error" rider exists only as prose — LAMBDA_EXPR is unlowered behind E129 and **no open issue owns lambda lowering** (all 137 open issues checked). |
| Delegated batch ruling — coordinator recommendations adopted, NOT FULLY REVIEWED | 2026-07-19 | DELIVERED | Merged PRs #1144/#1134/#1142/#1631; the "not fully reviewed" markers are present in the docs; F30/F31/F32 and batch-4 Q1/Q3/Q6/Q7 all verified in code; the #1101 follow-up shipped 2026-07-27. **Caveat that survives:** these remain sight-unseen adoptions — a downstream surprise should be escalated, not assumed deliberate. |
| Doc comments ruled first-class on the native surface | 2026-07-20 | DELIVERED | Merged PR #1218; `brink-syntax-native/src/lexer/mod.rs:257-264` non-trivia `///`/`//!`; DOC_COMMENT node as leading child; 13 `.doc()` accessors; **the ruled shared-parser factoring happened** — both frontends feed `hir::doc_block::parse_lines`. |
| `flow main()` ratified as the native story-entry convention; first light | 2026-07-21 | DELIVERED | Merged PR #1221; `brink-ir/src/hir/lower_native/mod.rs:279-286` `entry_root_content`; the whitespace bug fixed (#1336 closed) with policy documented; first_light now asserts episode-identity for all 9 fixtures. One stale doc line in the charter. |
| Flows end implicitly (native): fall-through = DONE, `-> END` stays explicit | 2026-07-22 | DELIVERED | `brink-ir/src/hir/lower_native/body.rs:443` `apply_implicit_done`, gated to non-function untyped bodies; E150 fall-through error; E151 dead-end lint (merged PR #1575). **Deviation:** E151 shipped on-by-default Warning rather than the ruled opt-in, flagged in-source for sign-off on an issue that has since closed. |
| Native project root: brink.toml's directory defines `story::`; rootless is a single-file project | 2026-07-22 | DELIVERED | Merged PR #1290; `brink-driver/src/source_tree.rs:258` `native_source_root` with walk-up and entry-dir fallback; module identity a pure function of the root-relative key. Deferral holds (no `source` key). Gap: the single-file mode is documented only in rustdoc, not the book. |
| Native source-loading seam: a `SourceTree` trait with a map-backed impl | 2026-07-22 | DELIVERED | Merged PRs #1284/#1290/#1327; `brink-source-tree/src/lib.rs:195` trait + `InMemory`; `RealFs`/`GitRev` in brink-driver; FileIds mint in sorted-key order. Signature refined (#1371): root is constructor-held, still caller-supplied. |
| Native grammar supports flat `else if` chains | 2026-07-22 | DELIVERED | Merged PR #1271; `brink-syntax-native/src/parser/family.rs:131` `at_else_arm` learned KW_IF; both spellings covered by tests + a proptest. "Lowers identically" (nested `InitialCondition`, not a flat multi-branch list) is now pinned by `brink-ir/tests/b07_native_body.rs::else_if_chain_lowers_to_nested_initial_condition`. Three stale doc comments claiming this grammar had no `else if` chain (`lower_native/cond.rs`, `hir::emit_native`'s module doc, `b07_native_body.rs`) were corrected in #1951's 2026-08-01 re-triage — see `docs/native-grammar-holes-triage-1951.md`'s "Hole 5" section. |
| Native content: alternation markers win over interpolation | 2026-07-22 | DELIVERED | `brink-syntax-native/src/parser/family.rs:40-41` `at_alternation` is the sole gate at every dispatch site; paren escape covered by tests. The ruling's pipe clause was superseded the same day by the stopping-sequence correction, which the code implements. |
| Native content: a `<-` outside a choice point is a warning, not silent prose | 2026-07-22 | DELIVERED | Merged PR #1269; `brink-syntax-native/src/parser/choice.rs:168-181` `p.warning(...)` with a confidence escalation; dispatched from both body paths (a dropped arm was restored in `6b2e54f5a`); test pins severity, count, message and unchanged CST shape. |
| Native decl: empty `flags` is `flags F = ()`; a bare `flags F =` is an error | 2026-07-22 | DELIVERED | Merged PR #1270; `brink-syntax-native/src/parser/decl.rs:263-299` accepts `()` before the member loop and `error_recover`s on zero progress — the asymmetric silent-accept path is gone. |
| Native content: empty alternation `{~}` / `{&}` is an error | 2026-07-22 | DELIVERED | Merged PR #1271; `brink-syntax-native/src/parser/family.rs:284-285` errors when no branch was seen, covering the multiline `{&\n}` case; comment cites the brink-syntax parity tests. |
| Native content: `{\|…}` is always a stopping-sequence; no malformed-lambda concept | 2026-07-22 | DELIVERED | Merged PR #1272; `family.rs:401-433` `inline_alternatives` has no space-after-separator branch left, with the removal recorded in the doc comment. Coverage gap: the ruling's literal `{\|heads\| tails}` strings are not used in any test. |
| Native decl: `use { a, b }` (bare group, no leading path) is an error | 2026-07-22 | DELIVERED | Merged PR #1283; `brink-syntax-native/src/parser/decl.rs:434` = `p.error("a `use` needs a module path".into())`, the exact ruled text; the dead bare-group branch pruned; `at_use_decl` left as required. |
| Native module identity: pure function of the root-relative path; FileId never in DefinitionId | 2026-07-22 | DELIVERED | Merged PR #1287; `brink-db/src/queries/mod.rs:642` inserts native modules bypassing `resolve_modules`; `modules.rs:99` is a pure string function with a save-key warning; FileId-independence directly tested by re-registering behind an earlier sibling. |
| Native discovery ignores non-`.brink` files in the source tree | 2026-07-22 | DELIVERED | Merged PR #1290; `brink-driver/src/source_tree.rs:100` `continue`s silently, with two tests planting `.ink`/`.md`/`brink.toml`. Tension noted: `discover_native` hard-errors on a non-native key from a hand-built tree — unreachable for every production caller (all pass `RealFs`). |
| CI consolidation: 29 jobs → ~12 by collapsing fuzz-smoke and sub-minute checks | 2026-07-22 | DELIVERED | Merged PR #1242; `ci.yml:356` one `Fuzz (smoke)` job looping 11 targets and recording all failures; `ci.yml:241` `Static checks` folding six checks with `if: !cancelled()`. **Two deviations:** `cargo-deny` was NOT folded (still standalone at `ci.yml:331`, and separately required by branch protection), and the actual count is 16 jobs, not ~12. *(Traced twice.)* |
| Code-ground sitting — RustScript statements, blocks-as-values, UFCS, `until` | 2026-07-23 | DELIVERED (partial) | Merged PRs #1302/#1321/#1328/#1497; items 1, 3, 4/6/7 and 5 are in main (`until_stmt`→`AwaitStmt`, E105 purity gate, `await` never in the native lexer). **Two sub-commitments not built:** blocks-as-values is still E129 (`lower_native/expr.rs:137`), owned by open #1665; time-waits-unify-into-`until` has no clock binding, owned by open #1293. |
| Native multi-file linking: codegen closure = the discovered module set (#1296) | 2026-07-23 | DELIVERED | Merged PR #1313; `brink-db/src/queries/mod.rs:500` documents native closure = every discovered `.brink` module by FileId while ink keeps the INCLUDE closure. Both ruled consequences pinned by tests. Reachability-based elimination correctly absent. |
| bevy-brink config discovery: brink.toml read-as-asset, bounded walk-up (#1029) | 2026-07-23 | DELIVERED | Merged PRs #1320, #1362; `bevy-brink/src/source_loader.rs:397` `probe_brink_toml` bounded ancestor walk, `read_asset_bytes` registering the hot-reload dependency, plugin override winning. Mechanism has since moved onto the brink-environment producer. |
| Compilation environment as a deterministic, serializable input (#1306) | 2026-07-23 | DELIVERED | Merged PR #1354; `brink-environment/src/lib.rs:169` `Environment` (content-hashed, reserved `resolved_deps`), `:305` effectful `Project::load`, `:468` pure `compile`; config walk-up moved onto the SourceTree seam (#1312 closed). Umbrella #1306 stays open for remaining mount instances. |
| SourceTree misfiled in brink-db → extract leaf `brink-source-tree` | 2026-07-23 | DELIVERED | Merged PR #1327; trait + `InMemory` now in the L0 leaf, brink-db re-exports, project-config depends on the leaf — cycle broken. **The ruling misstated where `RealFs`/`GitRev` lived** (already brink-driver before the extraction), not a code violation. Design issue #1323 still OPEN four days after its work merged. |
| Collection/construction initializer: `TypeName { }` is a std-registered construction protocol | 2026-07-23 | DELIVERED | Merged PR #1470 (#1464 closed, #1103 closed); `brink-ir/src/hir/construct.rs:75` closed `ConstructTarget` (Map/Flags/Weighted) with unregistered names falling through to structs; duplicate map keys are E138. Three deferrals honoured. **Deviations:** the "4th protocol entry" lives in brink-ir HIR, not the analyzer registry (which is still `ALL: [Protocol; 3]`), and the code is E138 not "E076-lineage". |
| Native prose dialect to be rethought from the ground up (#1351) | 2026-07-24 | DELIVERED | The round convened and ran (sittings 1–5 + four addenda; merged PRs #1441/#1447/…), producing `docs/prose-dialect-spec.md`; the editor track is confirmed held (#1131/#1350 open). **Three gaps, none owned:** the dissolved gather is never mentioned in the spec (`grep -ci gather` = 0), the `~`/`>` escapes get no keep/change ruling, and `docs/native-surface-charter.md` §5/§8 were never annotated as superseded. |
| Done vs End resolved by host lifecycle, not conformance (#1450) | 2026-07-25 | DELIVERED | Merged PR #1468; `brink-runtime/src/story/types.rs:35`/`:44` keep both terminals with host-lifecycle semantics; bevy dispatches them to distinct events. #1450 CLOSED with the resolution recorded. Caveat: the enum is still `Line`; whoever lands `Step` must carry both variants forward. |
| `x or y` short-circuits — the landed eager evaluation is a defect (#1471) | 2026-07-26 | DELIVERED | Merged PR #1479; `brink-runtime/src/vm.rs:1300` `Opcode::CoalesceSome(rel)` jump-based lowering; the eager pin flipped to a counter assertion. Two author-flagged follow-ups remain untracked, including that byte 0xFB changed encoding shape with no format-version signal. |
| The `as` binding: one construct, both condition positions, `{if}` spelling | 2026-07-26 | DELIVERED | Merged PR #1498; `brink-syntax-native/src/parser/binding.rs:1-70` one grammar rule / one node kind; whole-condition restriction enforced twice (parse error + E145); immutability as E148 at three choke points; `OptionBind` + E147 + runtime backstop; the mandated docs fix landed. |
| The COW no-aliasing invariant is load-bearing — sweep it, state it first-class | 2026-07-26 | DELIVERED | Merged PR #1494; both deliverables landed (regression tests + `docs/runtime-spec.md:73-86` stating the invariant, cross-referenced). **Bookkeeping gap:** #1476 is still OPEN — the PR said only "Part of" — and its comment records two follow-ups with no issue filed. |
| UFCS resolution pass designed: type-directed, in the analyzer, five rulings | 2026-07-26 | DELIVERED | Merged PRs #1497, #1524; `brink-analyzer/src/ufcs.rs:1-52` names D1–D5 in the module doc; downstream LIR (#1514) and IDE (#1534/#1543/#1554) consume the verdict table. #1482/#1462 closed. The maintainer rider is honoured — D3 follow-up is open **#1483**. |
| Quick-docket closures (five rapid-fire rulings) | 2026-07-26 | DELIVERED | Merged PR #1500 (root-scope-only final gather with the non-root fence explicit); remove/remove_at split with an E149 migration diagnostic; view-ratio spec'd as bench-pinned; Weighted has no len/iteration surface. **Gap on item (5):** the holes re-home exists only as spec prose — NS-T charter #1131's body never mentions holes, release policy, or lints. |
| NG-C ruled: `: type` returns everywhere — the colon is the one annotation spelling | 2026-07-26 | DELIVERED | Merged PRs #1496, #1491; `brink-syntax-native/src/parser/decl.rs:131` `return_type_clause` is the only spelling parsed, with tests for `fn`/`flow` headers. As-built constraint not in the ruling: a return-typed header requires an explicit param list, to keep the prose firewall. Rider holds by absence. |
| Lowering consumes analyzer types, never re-derives — the coalesce ruling generalized | 2026-07-26 | DELIVERED | Merged PRs #1516, #1479; public `brink_analyzer::coalesce_types` seam; `brink-ir/src/lir/lower/expr.rs:176` reads the verdict rather than re-deriving; `rhs_is_option_shaped` exists nowhere in production code; the #1479 limitation is now a passing test. **Bookkeeping gap:** #1492 still OPEN despite the work landing. |
| R1 (c): naming is the opt-in — a labelled choice/gather gets name-derived identity | 2026-07-27 | DELIVERED | `brink-ir/src/hir/stamp.rs:107-127` — a label resolves via `lookup_label_id(qualify(scope, label))` and only falls back to the anonymous hash when no label exists. Pre-existing behaviour the ruling relies on, not new work. |

---

# SUPERSEDED (16)

A later ruling overrode it; the code follows the later ruling.

| Title | Date | Verdict | Evidence |
|---|---|---|---|
| Explicit registration for instanced flow variable scoping | 2026-03-04 | SUPERSEDED | Superseded twice: `docs/decision-log.md:243` (`FLOW VAR`, inverts the default) and `:1236` (scoped-state policy final). Code follows the later rulings: `format/definition.rs:135` `pub local: bool`; `World::from_globals(…, ResolvedPolicy::all_world())`. Shipped in merged PR #495. The one surviving clause (branch-free VM on scoping) is delivered. **Separate finding:** `docs/runtime-spec.md:172` still describes the never-built `FLOW VAR` surface and a linker range-partition that does not exist. |
| HIR source provenance via AstPtr | 2026-03-04 | SUPERSEDED | Superseded by the 2026-07-19 delegated batch ruling Q1(b); delivered as opaque `Provenance` + frontend resolver (issue #1148 closed, merged PR #1153); `brink-ir/src/provenance.rs:1-10` says so. The goal survives via the resolver trait. **Loose end:** `AstPtr` is still compiled and publicly exported with no consumer — dead public surface, no issue. |
| Rename brink-hir to brink-ir, add LIR for codegen | 2026-03-05 | SUPERSEDED | Split: LIR half DELIVERED; dual-backend half dead — `brink-codegen-json` removed by merged PR #548. **Caveat:** the superseding entry names only converter/json ingestion, never the `.ink.json` *emit* backend, so that was retired by the PR without an explicit ruling. Residual: `brink-ir/src/lir/mod.rs:10` still documents the JSON backend as live. |
| `FLOW VAR` keyword for per-instance variable scoping | 2026-03-05 | SUPERSEDED | Mechanism shipped, spelling did not: `FLOW VAR` has zero occurrences; the storage class is `#@local` (merged PR #495). The single instance bit exists end-to-end. **Two residual gaps:** `docs/runtime-spec.md:172` is stale, and the "GetGlobal doesn't branch" clause is contradicted by the CoW read-through chain (`world.rs:1028`/`:1039`) — a deliberate consequence of the 2026-07-06 ruling. |
| Conformance work loop for JSON corpus | 2026-03-06 | SUPERSEDED | The corpus it governed no longer exists — verified: no brink-json / brink-converter in `crates/internal/`. Live successor is `CLAUDE.md:41-51`. Replaced deliberately, not dropped. |
| Program ownership: borrowed references, Arc deferred | 2026-03-13 | SUPERSEDED | Resolved exactly as its own escape clause anticipated, by the 2026-07-06 scoped-flow-state ruling and merged PR #442. **The entry is stale:** still marked tentative with `Story<'p>` and no forward pointer. **Live gap found in passing:** the `LinkedBinary`/`LinkedLocale` split exists only in `docs/runtime-spec.md:71` and `:716-745`; the shipped runtime is one `Program` struct. Unowned drift. |
| XLIFF uses scope names as IDs, not definition IDs | 2026-03-14 | SUPERSEDED | Superseded **in part** by the 2026-07-26 entry: the `<unit id>` half reversed (merged PR #1594); the `<file id>` half stands and is delivered. #1442 CLOSED. Caveat carried forward: the new scheme is not rename-stable and the churn defect is standing and unfixed. |
| Expose runtime primitives for direct orchestration | 2026-04-24 | SUPERSEDED | Superseded by the 2026-07-06 entry / merged PR #445. `Context` no longer exists; the free functions were never extracted (inherent `FlowInstance` methods instead); the `pub` primitives clause was **never delivered and is now deliberately reversed** — `CallFrame`/`CallStack`/`Thread`/`Flow` were never public at any point in main's history. The WHY is satisfied by a different mechanism. Do not re-file. |
| Default bevy wiring: Resources for shared state, Components for flows | 2026-04-24 | SUPERSEDED | Two of three placements replaced by later rulings (Context → `World`/`FlowLocal`; LineTables → `BrinkCurrentLocale<M>` + `BrinkLocale<M>`, i.e. the opt-in escape hatch became the default). The FlowInstance-on-a-Component placement still holds, and "no merge helpers" holds more strongly than in 2026-04. |
| Player may become an editor document rather than a tool window | 2026-06-10 | SUPERSEDED | Logged tentative and explicitly superseded the same day by "Player-as-document confirmed". Its conditional clause is moot — `mount.tsx:433-437` records "The old player tool window is gone". |
| #411 scratch-eval: proper deep-layer build (fragment codegen + program overlay) | 2026-07-06 | SUPERSEDED | The 2026-07-07 F5 entry names this ruling and drops its mechanism. Code follows the successor: no `OverlayProgram`/`ProgramLike` anywhere; the shipped path is `brink-web/src/compile.rs:213`. `evaluateScratch` never shipped either. Only the 0.9.0 release framing survived. **#411 still OPEN though the capability shipped — worth a triage pass.** |
| Tier-1 closures: env rows of val/ref captures | 2026-07-11 | SUPERSEDED | Superseded twice in the same direction (2026-07-12 partial application; 2026-07-23 by-value-only lambdas). `brink-syntax-native/src/parser/expr.rs:272-274` states `ref` is never accepted. The value *shape* survived as `ClosureValue` with a per-entry `is_ref`. The open cross-flow item is tracked: the fault is implemented, and #597 closed into open #1210. |
| Struct declaration syntax amended (native-surface reading) | 2026-07-13 | SUPERSEDED | DELIVERED for the ink dialect (separate row). Flagged separately because it is no longer the go-forward spelling on **native**: `brink-syntax-native/src/parser/decl.rs:317-318` documents `struct Name { field: Type, … }` — lowercase, no `=`, no `#`. Supersession chain is in the log ("Closers ruled" one-initializer-grammar; "Lambdas ruled" Rust-shaped). Two-surface divergence; the 2026-07-13 entry carries no forward pointer. |
| Vector math types: structs-first; sequence slices/ranges as range-projections | 2026-07-15 | SUPERSEDED | Part (1) overturned four days later by the tower ruling (glam-backed native Value kinds; `docs/tower-mini-spec.md:3-5` says it resolves the #827 question). Code follows the later ruling (`format/inkb/mod.rs:151-155`). **Supersession not propagated:** icebox #827 is still OPEN and `docs/native-surface-charter.md:174`/`:202` still list the #827 vec decision as open. Part (2) (reserved range segment) is delivered, though `docs/format-v4-rfc.md:32` never mentions the reservation. |
| Block/effect model — scoping correction + minimal-orthogonal-core north star | 2026-07-20 | SUPERSEDED | Two of three commitments overridden by the 2026-07-21 distillation entry (S2 dropped; #1213 downgraded from sequenced north star to guiding value). Evolve-Block-in-place holds; S1 and S5 landed. **Caveat:** #1213's body on GitHub still carries the pre-supersession text and was never edited. |
| Native interleaving & body-dialect spelling: `~`=enter-code, `>`=emit-prose | 2026-07-23 | SUPERSEDED | Reopened in full by the 2026-07-24 #1351 entry, *including* the `~`/`>` line escapes and `~{`/`>{` selectors. Whole-body selectors ARE built (`brink-syntax-native/src/parser/decl.rs:137`, merged PR #1329). **The line/block escapes are not built and no issue owns them** — #1309 closed 2026-07-27 as "already delivered" despite a comment recommending a follow-up; the only nominal owner is #1351, a design round rather than a parser build. |

---

# UNKNOWN (0)

No row was closed out as UNKNOWN. Several rows carry UNKNOWN *sub-clauses*, called
out in their notes rather than promoted to a separate verdict — the most consequential:

- **"Scripting-epic direction"** (2026-07-10): the "LIR logic/narrative split hygiene"
  sub-item — no commit or PR performing it was found; the module split predates phase 0.
- **"Entity flows persist state-only via per-flow SaveState"** (2026-07-10): the
  "state-only, resumes from knot entry not mid-line" clause was not verified in code.
- **"Layered execution model"** (2026-03-04): the glue-lookahead sub-clause at the
  line-continuation layer could not be located.
- **"brink-runtime stays bevy-free"** (2026-04-24): the exhaustive
  "every public type is Send + Sync + 'static" claim has no static assertion enforcing it.
- **"Phase 7 execution autonomy"** (2026-06-14): the review-gate half is not
  distinguishable from self-merge in the GitHub record.
