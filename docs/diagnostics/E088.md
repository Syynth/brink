# E088

## Summary

A bare-form `IMPORT { name } FROM mod` / native `use mod::name;` whose trailing segment `name` names neither a definition `mod` publicly exports **nor a declared submodule of `mod`** — `use`/`IMPORT` dual-reads its trailing segment (issue #1592): if `name` names a real submodule instead of an item, that reading licenses the submodule and this code does not fire. Only enforced against *declared* modules — an import naming an unknown/undeclared module is not itself flagged by this code, since that module's export/submodule set isn't visible to the check (modules-spec §2/§7).

## Explanation

[Detailed explanation of this diagnostic, why it occurs, and what it means for your code.]

## Example

```ink
[Minimal example demonstrating this diagnostic]
```

## Fix

[How to fix this issue.]

## See Also

- [DiagnosticCode documentation](../compiler-spec.md#diagnostic-codes)
