# E013

## Summary

RETIRED — `parser/divert.rs::path` always creates a `PATH` node (empty on error + E037), so `ThreadStart::target()` is never `None`. Code kept reserved, not reused.

## Explanation

## Example

## Fix

## See Also

- [DiagnosticCode documentation](../compiler-spec.md#diagnostic-codes)
