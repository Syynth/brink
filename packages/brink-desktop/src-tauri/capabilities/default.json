{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "brink-desktop main window: core + the folder-picker dialog. File I/O goes through the shell's own guarded commands, not the fs plugin. `core:window:allow-destroy` is not in `core:window:default` (#2370) — the quit path needs it to force-close the window after the awaited save, bypassing another `closeRequested` round-trip.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "core:window:allow-destroy"]
}
