# packages/brink-studio/src/__tests__/workspace-roots.ts parses the
# "packages:" list below to derive save-path-enrolment.test.ts's scan roots
# (issue #2515) — a new glob here, or a shape other than "<dir>/*", needs
# that parser taught about it or the enrolment suite fails loudly.
packages:
  - "packages/*"
