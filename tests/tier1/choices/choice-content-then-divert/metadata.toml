description = "Choice with its own emitted content followed by a divert to a new knot. The chosen choice's content (\"Picked.\\n\") and the divert target's first line (\"After divert.\\n\") should appear as separate transcript lines, not merged via glue."
mode = "runtime"

[source]
origin = "bevy-brink-conformance"
notes = "Surfaced from the play_story bevy-brink example: a choice with content immediately diverting to another knot rendered as one merged line in brink, but C# ink produces two separate paragraphs. Awaiting oracle generation via tools/ink-oracle to lock in the expected behavior."

[classification]
tier = 1
feature = "choices"
tags = ["choices", "divert", "newline", "needs-oracle"]
