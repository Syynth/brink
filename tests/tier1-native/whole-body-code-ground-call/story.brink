var n = 0

fn shout() >{
  Hi
  return
}

fn bump_and_shout() {
  n += 1;
  shout();
  n += 1;
}

fn tail_after_call() {
  shout();
  > tail
}

flow main() ~{
  > before
  shout();
  > middle
  bump_and_shout();
  > after {n}.
  tail_after_call();
}
