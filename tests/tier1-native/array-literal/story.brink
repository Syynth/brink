fn sum_all() {
  let items = [1, 2, 3];
  let total = 0;
  for x in items {
    total = total + x;
  }
  return total;
}

fn empty_count() {
  let items = [];
  let n = 0;
  for x in items {
    n = n + 1;
  }
  return n;
}

flow main() {
  Sum: {sum_all()}
  Empty: {empty_count()} -> END
}
