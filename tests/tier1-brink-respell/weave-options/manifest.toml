# INTERIM hand-respelled fixture. See ../README.md.
ink_source = "tests/tier1/weave/weave-options/story.ink"
oracle_case = "tier1::weave::weave-options"
family = "weave"
status = "hand-respelled, parse-verified only"
notes = """
Single-choice weave with no separate body content: the ink knot `test`
becomes a nested `flow`. The bare `-> END` after the closed `{? }` block is
the dissolved-gather respelling (charter §5): 'the next line after the
block' replaces ink's implicit gather that followed the one-line choice.
No gaps encountered.
"""
