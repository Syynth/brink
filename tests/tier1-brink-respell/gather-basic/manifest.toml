# INTERIM hand-respelled fixture. See ../README.md.
ink_source = "tests/tier1/gather/gather-basic/story.ink"
oracle_case = "tier1::gather::gather-basic"
family = "gather (dissolved)"
status = "hand-respelled, parse-verified only"
notes = """
The canonical dissolved-gather case (charter §5, "the single most
identity-altering respelling in this charter"): ink's unlabeled `-` gather
line becomes plain content immediately after the closed `{? }` block --
no gather sigil, no depth-matching squint. Each choice's multi-line body
moves into a brace opened on the choice's own line (charter: "choice
bodies take braces when they have nested content"). No gaps encountered.
"""
