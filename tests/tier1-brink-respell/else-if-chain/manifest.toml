# Mechanically generated by `respell_ink_source` (issue #1975) — checked in
# verbatim, not hand-edited. See ../README.md.
ink_source = "tests/tier2/conditional/ifelse-ext/story.ink"
oracle_case = "tier2::conditional::ifelse-ext"
family = "conditional"
status = "mechanically respelled, episode-identity proven"
notes = """
The ink origin's independently-chained 3-way conditional (`{ - x == 0: … -
x > 0: … - else: … }`, no shared switch subject) lowers to
`CondKind::IfElse` — a flat branch list with no native counterpart to walk
into directly (`lower_native::cond::lower_conditional`'s doc). Before issue
#1975, `emit_conditional`'s `CondKind::IfElse` arm refused this shape
outright (`full_corpus_sweep.rs`'s "IfElse conditional" bucket).
`emit_if_else_chain` now re-shapes the flat list into the same nested
`{if …} else { {if …} else { … } }` structure a native-authored `else if`
chain already lowers to on the way in, which is exactly the `story.brink`
checked in here.

Round-tripping *this* checked-in `story.brink` through native lowering
(`round_trip.rs`) only proves the emitted nested shape is itself legal,
episode-identical native source — native's own parser always constructs
`CondKind::InitialCondition` for an `if`/`else`/`else if` chain (never
`IfElse`, per the doc above), so this directory's round trip does not by
itself exercise the new `CondKind::IfElse` arm. The differential that
actually reaches it is `brink-respell`'s `ink_corpus_convert.rs::
ifelse_ext_three_way_chain`, which feeds the ink origin (not this
`story.brink`) through the ink frontend to construct the real `IfElse` HIR
shape, then asserts the mechanically emitted `.brink` plays
episode-identical to the ink original.
"""
