description = "A stateful sequence LEADING a multi-construct line (sequence, then inline conditional, then a second sequence) advances differently across repeated views (#3401)"
mode = "runtime"

[source]
origin = "brink"
original_id = "sequence-leads-multi-construct-line"
notes = "Expected to mismatch brink today (#3401) — oracle: apc, bpd, bpe, bpe, ...; brink: apc, bpc, bpd, ... Added to lock in the C#-oracle-correct behavior as a permanent regression target, not counted toward RATCHET_EPISODE_COUNT until fixed."

[classification]
tier = 2
feature = "sequences"
tags = ["sequences", "lifted-branches", "conditional", "known-mismatch"]
