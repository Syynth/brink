description = "A stateful sequence LEADING a multi-construct line (sequence, then inline conditional, then a second sequence) advances differently across repeated views (#3401)"
mode = "runtime"

[source]
origin = "brink"
original_id = "sequence-leads-multi-construct-line"
notes = "Added as an expected mismatch (#3401): brink advanced apc, bpc, bpd where the oracle advances apc, bpd, bpe. Fixed by having every clone of the sequence count on the original's container (whole-line renderings kept); counts toward RATCHET_EPISODE_COUNT since the 5619 -> 5622 raise."

[classification]
tier = 2
feature = "sequences"
tags = ["sequences", "lifted-branches", "conditional"]
