description = "A sequence, a function call, and an inline conditional lifted together on repeated visits; the function's side effect must be observed in call order (#3395)"
mode = "runtime"

[source]
origin = "brink"
original_id = "lift-order-seq-fn-cond"
notes = "Locks in the C#-oracle-correct behavior as a permanent regression target for #3395 (fixed: the lift now hoists every prefix interpolation into a synthetic temp ahead of the construct, so the condition sees the call's side effect)."

[classification]
tier = 2
feature = "evaluation"
tags = ["evaluation", "lift-order", "sequences", "function", "conditional"]
