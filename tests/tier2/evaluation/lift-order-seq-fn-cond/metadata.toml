description = "A sequence, a function call, and an inline conditional lifted together on repeated visits; the function's side effect must be observed in call order (#3395)"
mode = "runtime"

[source]
origin = "brink"
original_id = "lift-order-seq-fn-cond"
notes = "Expected to mismatch brink today (#3395) — added to lock in the C#-oracle-correct behavior as a permanent regression target, not counted toward RATCHET_EPISODE_COUNT until fixed."

[classification]
tier = 2
feature = "evaluation"
tags = ["evaluation", "lift-order", "sequences", "function", "conditional", "known-mismatch"]
