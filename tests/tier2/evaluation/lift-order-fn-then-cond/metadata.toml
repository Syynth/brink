description = "A function call lifted ahead of an inline conditional that reads the function's side effect; order of evaluation matters (#3395)"
mode = "runtime"

[source]
origin = "brink"
original_id = "lift-order-fn-then-cond"
notes = "Expected to mismatch brink today (#3395) — added to lock in the C#-oracle-correct behavior as a permanent regression target, not counted toward RATCHET_EPISODE_COUNT until fixed."

[classification]
tier = 2
feature = "evaluation"
tags = ["evaluation", "lift-order", "function", "conditional", "known-mismatch"]
