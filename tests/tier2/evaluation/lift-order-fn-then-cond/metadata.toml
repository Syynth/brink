description = "A function call lifted ahead of an inline conditional that reads the function's side effect; order of evaluation matters (#3395)"
mode = "runtime"

[source]
origin = "brink"
original_id = "lift-order-fn-then-cond"
expected_mismatch = "#3395"
notes = "Locks in the C#-oracle-correct behavior as a permanent regression target for #3395. See `expected_mismatch` above — the oracle harness (issue #3402) reads that field, not this note, to exclude this case's episodes from RATCHET_EPISODE_COUNT until it passes."

[classification]
tier = 2
feature = "evaluation"
tags = ["evaluation", "lift-order", "function", "conditional", "known-mismatch"]
