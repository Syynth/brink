description = "A function call lifted ahead of an inline conditional that reads the function's side effect; order of evaluation matters (#3395)"
mode = "runtime"

[source]
origin = "brink"
original_id = "lift-order-fn-then-cond"
notes = "Locks in the C#-oracle-correct behavior as a permanent regression target for #3395 (fixed: the lift now hoists every prefix interpolation into a synthetic temp ahead of the construct, so the condition sees the call's side effect)."

[classification]
tier = 2
feature = "evaluation"
tags = ["evaluation", "lift-order", "function", "conditional"]
