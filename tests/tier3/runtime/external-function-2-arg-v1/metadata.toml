description = "blade-ink-rs: external_function"
mode = "runtime"

[source]
origin = "blade-ink-rs"
original_id = "external_function"

[classification]
tier = 3
feature = "runtime"
tags = []

[skip]
reason = "requires custom external function handler (transcript expects handler result, not fallback)"
